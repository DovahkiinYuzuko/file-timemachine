# ハリボテリスト (TODO List)

最新更新日: 2026-05-20 (アビーのコード監査によるアップデート)

ファイルタイムマシンを完成させるために、今現在見た目だけあるいは動かない箇所をまとめたよ。
これを一つずつ、ユズコと一緒に丁寧に潰していこうね！

## 1. コア機能 (Git とファイル操作)
- [x] **ファイルツリーの実装**: 
  - 現在: 本物のディレクトリ構造が表示されるようになったよ！Rust側の `get_file_tree` コマンドとフロントエンドの `FileTree.tsx` が連動して動作しているよ。
- [x] **履歴 (コミット) リストの動的表示**:
  - 現在: 実装完了！Rust側の `git_log` コマンドから取得した実際の履歴がテーブル形式で表示されるようになったよ。
- [x] **Gitグラフ (SVG) の描画と切り替えUI**:
  - 現在: `GitGraph.tsx` が完成し、ツリー形式と路線図形式の切り替えが可能なコンポーネントが動作するよ。
- [x] **GitGraphコンポーネントのMainLayoutへの組み込み**:
  - 現在: 実装完了！MainLayoutにGitGraphが組み込まれ、実際のデータ（プロジェクトパス）と完全に連動してSVG路線図が描画されるようになったよ。
- [ ] **今の状態を保存する (Save / Commit) 本実装**:
  - 現在: Rust側の `commands/git.rs` には本物の `git_commit` コマンド（`git add .` と `git commit` を実行するもの）が用意されているけれど、フロントエンドの `MainLayout.tsx` からは呼び出されていなくて、`alert` によるデモ表示にとどまっているよ。
  - ゴール: フロントエンドの保存ボタンからRustの `git_commit` を呼び出し、実際にコミットを作成・保存できるようにする。
- [ ] **お試しルート機能 (Trial / Branching)**:
  - 現在: Rust側の `git.rs` にもブランチ作成のコマンドが存在せず、フロントエンドにもUIボタンがないよ。
  - ゴール: 「新しいルートを作る」ボタンを設置し、Rust側にブランチ作成コマンドを実装して連携させる。
- [ ] **ルートの切り替え機能 (Checkout)**:
  - 現在: 過去の履歴や別ルートに移動するUIや、Rust側のチェックアウトコマンドが未実装だよ。
  - ゴール: 履歴リストやグラフから特定の状態を選択し、Rust側のチェックアウトコマンドを実行してフォルダ状態を切り替えられるようにする。
- [ ] **本番に採用する (Merge)**:
  - 現在: お試し案を本番（メイン）に合流させるマージ機能が未実装だよ。
  - ゴール: 「本番に採用」ボタンを設置し、Rust側でマージコマンドを実行する。
- [ ] **中身で探す機能 (Search / Grep)**:
  - 現在: UI、バックエンドともに未実装だよ。
  - ゴール: 「過去の全記録から検索」する検索窓をUIに追加し、裏で `git grep` などを実行して結果を返すRustコマンドを実装する。
- [ ] **ネットの金庫とやり取り (Sync / GitHub Push/Pull)**:
  - 現在: UI、バックエンドともに未実装だよ。
  - ゴール: 同期ボタンを配置し、リモートリポジトリとのPush/Pullを自動化する。

## 2. UI/UX とデザイン
- [x] **ファイルプレビュー機能**:
  - 現在: 判定エンジン完成！テキスト、画像、動画などのプレビューが可能。テキストファイルはRust側の `read_file_content` で自動エンコーディング検知を行っているよ。
- [x] **プロジェクト単位の管理モード実装**:
  - 現在: ホワイトリスト/ブラックリストの切り替えが `.file-timemachine.json` に保存されるよ。
- [x] **ライトテーマの適用**:
  - 現在: 設定で切り替え可能になったよ！
- [x] **設定画面の言語切り替えバグ修正**:
  - 現在: 言語を変えた瞬間に即座に反映されるようになったよ！
- [ ] **設定画面の値とアプリ挙動の実連携**:
  - 現在: "ルート切り替え時の保存設定" 等が localStorage に保存されるだけで、実際の挙動には影響していないよ。
  - ゴール: 自動保存などの設定値をアプリケーションの実際の制御ロジックに組み込む。

## 3. 拡張機能
- [ ] **GitHub OAuth ログインの完遂**:
  - 現在: ブラウザを開くだけで、ログイン後の認証情報を受け取って保存する処理がないよ。
  - ゴール: ログイン後のトークンを受け取り、安全に保存して、GitHubとの同期ができるようにする。
- [ ] **本物の安全ガード (Safety Scan)**:
  - 現在: フロントエンドの `utils/safety.ts` には本物の検知ロジック（.envや大容量ファイルの検出）が実装されているけれど、`MainLayout.tsx` から呼び出す際にダミーのファイルリストを渡して動作をエミュレートしているデモ状態だよ。
  - ゴール: 保存する前に、実際にステージング（保存）対象のファイルを走査してスキャンし、警告を出すようにする。
- [ ] **おまかせセットアップウィザードの本実装**:
  - 現在: 依存関係の有無を `check_dependencies` で診断して結果をウィザード画面に表示するだけだよ。
  - ゴール: ツールが不足している場合に、Homebrewやwinget等を使用して実際にインストールを代行するバックエンド処理を実装する。

---
少しずつ、一歩ずつ。最強のアプリを一緒に作っていこうね、ユズコ！
