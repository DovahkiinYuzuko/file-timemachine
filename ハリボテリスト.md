# ハリボテリスト (TODO List)

最新更新日: 2026-05-22 (GitHub OAuth連携・リポジトリ自動作成＆Push/Pull同期・GitGraphマルチブランチ表示完了！💅✨)

ファイルタイムマシンを完成させるために、今現在見た目だけあるいは動かない箇所をまとめたよ。
これを一つずつ、ユズコと一緒に丁寧に潰していこうね💅

## 1. コア機能 (Git とファイル操作)
- [x] **ファイルツリーの実装**: 
  - 現在: 本物のディレクトリ構造が表示されるようになったよ！Rust側の `get_file_tree` コマンドとフロントエンドの `FileTree.tsx` が連動して動作しているよ。
- [x] **履歴 (コミット) リストの動的表示**:
  - 現在: 実装完了！Rust側の `git_log` コマンドから取得した実際の履歴がテーブル形式で表示されるようになったよ。
- [x] **Gitグラフ (SVG) の描画と切り替えUI**:
  - 現在: 実装完了！GitGraph.tsx がバニラJS版に移行して React 19 の二重マウントバグに対応済み。
- [x] **GitGraphコンポーネントのMainLayoutへの組み込み**:
  - 現在: 実装完了！MainLayoutにGitGraphが組み込まれ、実際のデータと連動してSVGグラフが描画されるよ。
  - プレミアム改善: さらに、Rustバックエンドの `git_log` に `--all` を追加してマルチブランチの履歴表示にも完全対応！ダークモード時のヘッダー背景の白浮きバグも綺麗に解消し、ファイルツリーのバッジレイアウト崩れも完全に防止したよ！
- [x] **今の状態を保存する (Save / Commit) 本実装**:
  - 現在: 実装完了！保存ボタンから Rust の `git_commit` を呼び出し、実際にコミットを作成・保存できるよ。設定画面の `saveBehavior` とも連動済み。
- [x] **お試しルート機能 (Trial / Branching)**:
  - 現在: 実装完了！新しいブランチを作成し、別の作業ラインを並行して管理できるようになったよ。
- [x] **ルートの切り替え機能 (Checkout)**:
  - 現在: 実装完了！フッターから既存のルート（ブランチ）を一覧して、一瞬で切り替えられるようになったよ。
- [x] **本番に採用する (Merge)**:
  - 現在: **実装完了・ハリボテ卒業！** 💅✨
  - 詳細: 
    * 競合が発生した際、左右2ペインのリッチなプレビュー画面で内容を確認し、どちらを採用するか選べる「競合解決モーダル」を完全実装。
    * 日本語ファイル名のエスケープ問題や、バイナリファイルの誤読み込みも克服。
    * 解決を中止（キャンセル）した際の自動ロールバック機能により、リポジトリの安全性も確保。
- [x] **中身で探す機能 (Search / Grep)**:
  - 現在: 実装完了！ファイルツリーとコミット履歴の両方でキーワード検索が可能になったよ。
- [x] **ネットの金庫とやり取り (Sync / GitHub Push/Pull)**:
  - 現在: **実装完了・ハリボテ卒業！** 💅✨
  - 詳細:
    * 同期ボタンをクリックすると、裏で自動的にリモートリポジトリ（origin）から Pull し、その後に Push する連携フローを本実装。
    * Pull 時に競合が発生した場合は、すでに実装済みの「競合解決モーダル」が自動で立ち上がり、安全に解決してから同期を再開できるよう連携したよ！
    * origin が未設定の場合は、自動的にクラウド同期設定モーダルがポップアップして設定を促すから、超スムーズ！

## 2. UI/UX とデザイン
- [x] **ファイルプレビュー機能**:
  - 現在: 実装完了！テキスト、画像、リアルタイム更新、差分(Diff)表示、そしてバイナリ検出までフル装備だよ💅✨
- [x] **プロジェクト単位の管理モード実装**:
  - 現在: 実装完了！ホワイトリスト/ブラックリストの切り替え、日本語ファイル名対応、楽観的更新(Optimistic UI)でサクサク動くよ。
- [x] **ライトテーマの適用**:
  - 現在: 設定で切り替え可能になったよ！
- [x] **設定画面の言語切り替えバグ修正**:
  - 現在: 言語を変えた瞬間に即座に反映されるようになったよ！
- [x] **設定画面の値とアプリ挙動の完全連携**:
  - 現在: `saveBehavior` などの主要な設定が Tauri のネイティブ設定ファイル (`app_config.json`) と完全に同期しているよ。
- [x] **ウィンドウ状態の自動記憶と初期サイズ拡張**:
  - 現在: **実装完了！** 💅✨ デフォルト解像度を `1024x768` に広げ、さらに前回閉じた時のサイズや位置、最大化状態を自動復元する機能を追加したよ！

## 3. 拡張機能
- [x] **GitHub OAuth ログインの完成**:
  - 現在: **実装完了・ハリボテ卒業！** 💅✨
  - 詳細:
    * GitHub API を用いて、連携後にユーザーのアイコン（アバター画像）、ユーザー名、ログイン名をカード型UIで美しく表示。
    * PC にインストールされている GitHub CLI から認証トークンを一瞬で抽出して自動インポートする機能、および Personal Access Token (PAT) を安全に手動入力できるフォームを本実装したよ！
- [ ] **本物の安全ガード (Safety Scan)**:
  - 現在: フロントエンドにロジックはあるけど、実際の保存対象ファイルを全スキャンする部分はまだデモ状態。
- [x] **おまかせセットアップウィザードの本実装**:
  - 現在: **実装完了！** 💅✨ 不足しているツール（Git, GitHub CLI）を自動でダウンロード＆インストールするバックエンド処理（`install_dependency`）と、フロントエンドの詳細ログ表示・シミュレーションモードを完全実装したよ！

---
一歩ずつ,確実に。最高のアプリを一緒に作っていこうね、ユズコ！💅✨💖
