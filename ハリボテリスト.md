# ハリボテリスト (TODO List)

最新更新日: 2026-05-21 (マージ競合解決、多言語化・堅牢化対応完了！💅✨)

ファイルタイムマシンを完成させるために、今現在見た目だけあるいは動かない箇所をまとめたよ。
これを一つずつ、ユズコと一緒に丁寧に潰していこうね💅

## 1. コア機能 (Git とファイル操作)
- [x] **ファイルツリーの実装**: 
  - 現在: 本物のディレクトリ構造が表示されるようになったよ！Rust側の `get_file_tree` コマンドとフロントエンドの `FileTree.tsx` が連動して動作しているよ。
- [x] **履歴 (コミット) リストの動的表示**:
  - 現在: 実装完了！Rust側の `git_log` コマンドから取得した実際の履歴がテーブル形式で表示されるようになったよ。
- [x] **Gitグラフ (SVG) の描画と切り替えUI**:
  - 現在: 実装完了！GitGraph.tsx がバニラJS版に移行して React 19 の二重マウントバグに対応済み。
- [x] **GitGraphコンポーネントのMainLayoutへの組み込み**:
  - 現在: 実装完了！MainLayoutにGitGraphが組み込まれ、実際のデータと連動してSVGグラフが描画されるようになったよ。
- [x] **今の状態を保存する (Save / Commit) 本実装**:
  - 現在: 実装完了！保存ボタンから Rust の `git_commit` を呼び出し、実際にコミットを作成・保存できるよ。設定画面の `saveBehavior` とも連動済み。
- [x] **お試しルート機能 (Trial / Branching)**:
  - 現在: 実装完了！新しいブランチを作成し、別の作業ラインを並行して管理できるようになったよ。
- [x] **ルートの切り替え機能 (Checkout)**:
  - 現在: 実装完了！フッターから既存のルート（ブランチ）を一覧して、一瞬で切り替えられるようになったよ。
- [x] **本番に採用する (Merge)**:
  - 現在: **実装完了・ハリボテ卒業！** 💅✨
  - 詳細: 
    * 競合が発生した際、左右2ペインのリッチなプレビュー画面で内容を確認し、どちらを採用するか選べる「競合解決モーダル」を完全実装。
    * 日本語ファイル名のエスケープ問題や、バイナリファイルの誤読み込みも克服。
    * 解決を中止（キャンセル）した際の自動ロールバック機能により、リポジトリの安全性も確保。
- [x] **中身で探す機能 (Search / Grep)**:
  - 現在: 実装完了！ファイルツリーとコミット履歴の両方でキーワード検索が可能になったよ。
- [ ] **ネットの金庫とやり取り (Sync / GitHub Push/Pull)**:
  - 現在: UI、バックエンドともに未実装。
  - ゴール: 同期ボタンを配置し、リモートリポジトリとのPush/Pullを自動化する。

## 2. UI/UX とデザイン
- [x] **ファイルプレビュー機能**:
  - 現在: 実装完了！テキスト、画像、リアルタイム更新、差分(Diff)表示、そしてバイナリ検出までフル装備だよ💅✨
- [x] **プロジェクト単位の管理モード実装**:
  - 現在: 実装完了！ホワイトリスト/ブラックリストの切り替え、日本語ファイル名対応、楽観的更新(Optimistic UI)でサクサク動くよ。
- [x] **ライトテーマの適用**:
  - 現在: 設定で切り替え可能になったよ！
- [x] **設定画面の言語切り替えバグ修正**:
  - 現在: 言語を変えた瞬間に即座に反映されるようになったよ！
- [x] **設定画面の値とアプリ挙動の完全連携**:
  - 現在: `saveBehavior` などの主要な設定が Tauri のネイティブ設定ファイル (`app_config.json`) と完全に同期しているよ。

## 3. 拡張機能
- [ ] **GitHub OAuth ログインの完成**:
  - 現在: まだブラウザを開くだけ。ログイン後の認証トークンを受け取る処理が必要。
- [ ] **本物の安全ガード (Safety Scan)**:
  - 現在: フロントエンドにロジックはあるけど、実際の保存対象ファイルを全スキャンする部分はまだデモ状態。
- [ ] **おまかせセットアップウィザードの本実装**:
  - 現在: 診断結果を表示するだけ。不足しているツールを実際にインストールするバックエンド処理が必要。

---
一歩ずつ、確実に。最高のアプリを一緒に作っていこうね、ユズコ！💅✨💖
