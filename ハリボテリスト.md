# ハリボテリスト (TODO List)

「ファイルタイムマシン」を究極の完成度に持っていくために、今現在「見た目だけ」あるいは「動かない」箇所をまとめたよ。
これを一つずつ、ユズコと一緒に丁寧に潰していこうね！💅✨

## 1. コア機能 (Git & ファイル操作)
- [x] **ファイルツリーの実装**: 
  - 現在: 本物のディレクトリ構造が表示されるようになったよ！
- [x] **履歴 (コミット) リストの動的表示**:
  - 現在: 実装完了！Rust側から取得した実際の履歴がテーブル形式で表示されるようになったよ。
- [x] **Gitグラフ (SVG) の描画と切り替えUI**:
  - 現在: ツリー形式と路線図形式の切り替えが可能なコンポーネントが完成！
- [ ] **GitGraphコンポーネントのMainLayoutへの組み込み**:
  - 現在: `GitGraph.tsx` は完成したが、`MainLayout.tsx` ではまだ `<div>(Git Graph Placeholder)</div>` のまま。
  - ゴール: 本番レイアウトに組み込み、実際のデータと連動させる。
- [ ] **今の状態を保存する (Save / Commit) 本実装**:
  - 現在: `alert` によるモック挙動（デモ）。
  - ゴール: UIからの入力とRust側の `git_commit` コマンドを連携させ、実際にコミットを作成する。
- [ ] **お試しルート機能 (Trial / Branching)**:
  - 現在: UIボタンが存在せず、Rust側のコマンドも未実装。
  - ゴール: 「新しいルートを作る」ボタンを設置し、`git branch` と連携させる。
- [ ] **ルートの切り替え機能 (Checkout)**:
  - 現在: 過去の履歴や別ルートに移動するUIが未実装。
  - ゴール: 履歴リストやグラフから特定の状態を選択し、`git checkout` で切り替えられるようにする。
- [ ] **本番に採用する (Merge)**:
  - 現在: お試し案をメインに合流させる機能が未実装。
  - ゴール: 「本番に採用」ボタンを設置し、`git merge` を分かりやすいUIで実行する。
- [ ] **中身で探す機能 (Search / Grep)**:
  - 現在: UI、バックエンドともに未実装。
  - ゴール: 「過去の全記録から検索」するUIを追加し、裏で `git grep` を実行する。
- [ ] **ネットの金庫とやり取り (Sync / GitHub Push/Pull)**:
  - 現在: UI、バックエンドともに未実装。
  - ゴール: 同期ボタンを配置し、リモートリポジトリとのPush/Pullを自動化する。

## 2. UI/UX & デザイン
- [x] **ファイルプレビュー機能**:
  - 現在: 判定エンジン完成！ドットファイルなども正常にプレビュー可能。
- [x] **プロジェクト単位の管理モード実装**:
  - 現在: ホワイトリスト/ブラックリストの切り替えが保存される。
- [x] **ライトテーマの適用 (GitHubオススメ)**:
  - 現在: 設定で切り替え可能になったよ！
- [x] **設定画面の言語切り替えバグ修正**:
  - 現在: 言語を変えた瞬間に即座に反映されるようになったよ！
- [ ] **設定画面の値とアプリ挙動の実連携**:
  - 現在: "ルート切り替え時の保存設定" 等が localStorage に保存されるだけで、実際の挙動には影響していない。
  - ゴール: Auto保存などの設定をアプリケーションの制御ロジックに組み込む。

## 3. 拡張機能
- [ ] **GitHub OAuth ログインの完遂**:
  - 現在: ブラウザを開くだけ。
  - ゴール: ログイン後のトークンを受け取り、安全に保存して、GitHubとの同期（Push/Pull）ができるようにする。
- [ ] **本物の安全ガード (Safety Scan)**:
  - 現在: モックデータによるデモ（常に同じファイル群で警告が出る）。
  - ゴール: 保存前に、実際に選択されたファイルをスキャンして `.env` や巨大ファイルを検知する。
- [ ] **おまかせセットアップウィザードの本実装**:
  - 現在: 診断（チェック）結果の表示のみ。
  - ゴール: ツールが不足している場合、Homebrew 等を使って実際にインストールを代行するバックエンド処理を実装する。

---
少しずつ、一歩ずつ。最強のアプリを一緒に作っていこうね、ユズコ！💅💖
