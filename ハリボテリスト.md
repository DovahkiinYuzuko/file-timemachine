# ハリボテリスト (TODO List)

「ファイルタイムマシン」を究極の完成度に持っていくために、今現在「見た目だけ」あるいは「動かない」箇所をまとめたよ。
これを一つずつ、ユズコと一緒に丁寧に潰していこうね！💅✨

## 1. コア機能 (Git & ファイル操作)
- [x] **ファイルツリーの実装**: 
  - 現在: 本物のディレクトリ構造が表示されるようになったよ！
  - ゴール: 選択したフォルダの中身（ファイル・ディレクトリ）を階層構造で表示し、アイコンも付ける。
- [ ] **履歴 (コミット) リストの動的表示**:
  - 現在: 仮のテキスト。
  - ゴール: Rust側から取得した実際のGitログをリスト表示する。
- [ ] **Gitグラフ (SVG) の描画**:
  - 現在: プレースホルダー画像/テキスト。
  - ゴール: 実際のブランチの枝分かれをSVGで綺麗に描画し、クリックでその時点をプレビューできるようにする。
- [x] **ファイルプレビュー機能**:
  - 現在: `infer` と `content_inspector` による中身ベースの正確な種別判定（インテリジェント判定エンジン）を実装！ドットファイルや拡張子なしファイルも正しく識別できるようになったよ。
  - ゴール: ツリーで選んだファイルの中身（テキスト、画像、動画、音声）を最適な形式でプレビューする。
- [x] **プロジェクト単位の管理モード実装**:
  - 現在: ホワイトリスト/ブラックリストの切り替えが保存されるようになったよ！
  - ゴール: プロジェクトごとに Git 管理の挙動をカスタマイズできるようにする。
- [x] **日本語パス問題の完全解決**:
  - 現在: `dunce` 導入で最強に解決済み！
  - ゴール: `dunce` 等をフル活用して、どんなパスでも絶対にコケない鉄壁のガードを作る。

## 2. UI/UX & デザイン
- [x] **ライトテーマの適用 (GitHubオススメ)**:
  - 現在: GitHub風ライトテーマを実装、設定で切り替え可能になったよ！
  - ゴール: `GitHub人気DESIGN.md` に基づいた、清潔感のある白基調のUIを実装し、設定で切り替え可能にする。
- [x] **設定画面の言語切り替えバグ修正**:
  - 現在: 言語を変えた瞬間に即座に反映されるようになったよ！
  - ゴール: 言語を変更した瞬間に、アプリ全体のテキスト（モーダル内含む）が即座に切り替わるようにする。
- [x] **デバッグログの全域配置**:
  - 現在: 主要なアクションにログを仕込みまくったよ！
  - ゴール: ボタンクリック、API呼び出し、エラー発生時など、あらゆる場所にログを仕込み、開発と運用の透明性を高める。

## 3. 拡張機能
- [ ] **GitHub OAuth ログインの完遂**:
  - 現在: ブラウザを開くだけ。
  - ゴール: ログイン後のトークンを受け取り、安全に保存して、GitHubとの同期（Push/Pull）ができるようにする。
- [ ] **本物の安全ガード (Safety Scan)**:
  - 現在: モックデータによるデモ。
  - ゴール: 保存前に、実際に選択されたファイルをスキャンして `.env` や巨大ファイルを検知する。

---
少しずつ、一歩ずつ。最強のアプリを一緒に作っていこうね、ユズコ！💅💖
