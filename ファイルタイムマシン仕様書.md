# ソフトウェア製品仕様書：ファイルタイムマシン (File Time Machine)

## 1. プロジェクト概要
* **製品名:** ファイルタイムマシン
* **コンセプト:** 「Gitを意識させないGitクライアント」。非エンジニアが直感的に「過去の状態への復元」「複数案の並行作成」「内容の比較」を行えるようにする。
* **ターゲット:** Windows 11 / macOS を使用するクリエイターおよび事務職。

---

## 2. 技術スタック
* **フレームワーク:** Tauri (Rustベース)
* **フロントエンド:** React (TypeScript)
* **バックエンド:** Rust (Git/GitHub CLIのラッパー)
* **依存ツール:** Git CLI, GitHub CLI (`gh`)

---

## 3. 主要機能詳細

### ① スマート・ホワイトリスト初期化
* **挙動:** `git init` 実行後、`.gitignore` を「全拒否」設定（`*`）で作成。
* **設定:** ユーザーがUI上で「これだけGitに保存する（例：.docx, .psd, .xlsx）」みたいな選択をすると、それだけを逆gitignoreする設定を自動追記。
* **メリット:** プロジェクトフォルダがどれだけ汚れていても、大事な成果物だけをクリーンに管理できる。

### ② 今の状態を保存 (Save / Commit)
* **操作:** ボタン一つで全変更を記録。
* **自動化:** コミットメッセージは自動でタイムスタンプを付与。「14:00のバックアップ」のように表示。
* **内部処理:** `git add -A` → `git commit -m "[Timestamp]"`

### ③ お試しルート機能 (Trial / Branching)
* **操作:** 「新しいルートを作る」ボタンで分岐。
* **用途:** A案を維持したままB案を試す。ボタン一つでフォルダの中身をA案とB案で切り替える。
* **合体:** 試作が成功したら「今のルートを本番に採用」ボタンでメインに統合（マージ）。

### ④ 間違い探し機能 (Compare / Diff)
* **操作:** 履歴リストから2つの保存点を選択。
* **表示:** どのファイルのどの行が書き換わったか、追加されたかをハイライト表示。
* **対象:** テキストベースのファイルはもちろん、バイナリファイルも「更新あり」として検知。

### ⑤ 「あの時のアレ」検索 (Search / Grep)
* **操作:** キーワードを入力して検索。
* **挙動:** 今のファイルに書いていなくても、過去の全履歴の中からその言葉が含まれていた時点を探し出す。
* **内部処理:** `git grep [keyword] $(git rev-list --all)`

### ⑥ ネットの金庫に預ける (Sync / GitHub integration)
* **連携:** `gh` コマンドを使用してGitHubと同期。
* **自動化:** ブラウザ認証の呼び出し、非公開リポジトリの作成、プッシュまでを一貫してガイド。

### ⑦ おまかせセットアップウィザード
* **挙動:** アプリ起動時にGitの有無を自動診断。未インストールの場合は専用のウィザード画面を表示。
* **インストール:** ユーザーのOSを判別し、適切なパッケージマネージャー（winget/Homebrew等）を使用してGitを自動導入。
* **フィードバック:** インストール中の詳細ログと進捗バーをリアルタイムに表示。
* **安全:** インストール完了後に再診断を行い、正常動作を確認してからメイン画面へ遷移。

---

## 4. UI/UX デザインマッピング
エンジニア用語を徹底的に排除し、日常言語に変換する。

| Git用語 | アプリ内表記 | ユーザーの体感 |
| :--- | :--- | :--- |
| Commit | **保存する** | 今の状態を記録に残す |
| Branch | **お試しルート** | 本番を汚さず別案を作る |
| Checkout | **切り替える** | 過去や別ルートの状態に変える |
| Merge | **採用する** | お試し案を本番に反映させる |
| Diff | **比較する** | どこが変わったか確認する |
| Grep | **中身で探す** | 過去の全ての記録から言葉を探す |

---

## 5. 安全設計
* **自動避難:** 過去に戻ったりルートを切り替えたりする際、未保存の変更がある場合は「作業中バックアップ」として自動コミットしてから実行する。
* **競合（コンフリクト）の単純化:** ファイルが衝突した場合は、エンジニアのようなコード編集はさせず、「今のやつを優先」「戻すやつを優先」「両方別名で保存」の3択から選ばせる。

---

## 6. 実装時の監査ポイント (他AI向け)
* **OS差異:** WindowsのバックスラッシュとMacのスラッシュの扱いに矛盾が出ないか。
* **パフォーマンス:** ファイル数が多いフォルダでも、Rust側での変更検知を最適化してUIが固まらないようにすること。
* **パスの安全性:** フォルダパスに日本語やスペースが含まれていても、Gitコマンドが正常に動作するようにクォート処理を徹底すること。

---

## 7.私からの追記

- 普通に.gitignoreは作れていいんです。ブラックリスト方式化ホワイトリスト方式がユーザーが選べるようになるといいかなって思います。
- 作成時はアプリ本体の脆弱性チェックとかもよろしく！
- ユーザー環境にGitもGitHubCLIも入ってなかったら簡単にインストールできるようにしてあげて！
- GitHub連携はGitHubアカウント持ってる人だけでいいや。あとからOAuthみたいな感じでアカウント持ってきてもらえばいいでしょ。

↓これめっちゃいい！！↓
```markdown
## RakutenAI
ただし絶対に必要なガード
もし本当に作るなら、最低限これが欲しいです。

1. 破壊操作の前にプレビュー
何が消える/戻るか表示
2. 自動バックアップ
復元前に現在状態を自動保存
3. 秘密情報チェック
.env
APIキーっぽいファイル
大きすぎる生成物
node_modules
venv などを警告
4. 初回セットアップ診断
Git入ってるか
ユーザー名/メール設定あるか
GitHub CLIあるか
認証済みか
5. 「このフォルダは向いてない」警告
たとえば:

OS全体のホームディレクトリ
ダウンロードフォルダ丸ごと
巨大バイナリだらけ
クラウド同期中の危険フォルダ
このへんを弾けるとかなり親切です。
```
