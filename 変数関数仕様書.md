# 変数関数仕様書

このドキュメントは、プロジェクト内の重要な変数、関数、コンポーネントの仕様と依存関係を管理するためのものです。

---

## 1. コンポーネントおよび関数定義

### `GitGraph` (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx`
- **概要**: Gitのコミットログを取得し、路線図またはツリーとして可視化するReactコンポーネント。
- **Props**:
  - `projectPath: string | null` - 可視化対象のリポジトリパス。
  - `refreshKey?: number` - 更新をトリガーするためのキー値。
  - `onInitSuccess?: () => void` - リポジトリ初期化成功時のコールバック。

---

## 2. 変更・追加された関数仕様 (2026-05-20 更新)

### `getTemplate` (Internal Function)
- **概要**: `@gitgraph/js` のレンダリングテンプレートを生成するヘルパー関数。ズームレベル（`scale`）やグラフ表示スタイルに追従したスタイリングを動的に適用する。

#### 引数・戻り値の定義
- **引数**:
  - `style: GraphStyle` ("tree" | "metro") - レンダリングするグラフの種類。
  - `currentScale: number` - ズーム比率（`0.4` 〜 `2.0`）。
- **戻り値**:
  - `Template` - `@gitgraph/js` に渡すための拡張テンプレートオブジェクト。

#### 内部変更点
- `labelFontSize` (内部変数) を新設：`Math.max(8, Math.round(10 * currentScale))` で算出。
- `baseOptions` の `branch.label` プロパティを追加：
  - `display: true` (ブランチバッジを表示)
  - `font`: `normal ${labelFontSize}px ...` (ズームレベル連動の極小フォント)
  - `bgColor`: `"#ddf4ff"` (GitHub風の淡いブルー)
  - `color`: `"#0969da"` (視認性の高いダークブルー)
  - `strokeColor`: `"transparent"` (枠線の非表示化)
  - `borderRadius`: `10` (ピルケース形状の角丸)

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `currentScale` (引数 / 親コンポーネントの `scale` 状態)
  - `@gitgraph/js` の `templateExtend` および `TemplateName`
- **影響範囲 (Impact Scope)**:
  - `@gitgraph/js` の描画ロジック。
  - `GitGraph.tsx` 内の `useEffect` (マニュアルDOMレンダリングを実行するライフサイクル処理)。

---

## 3. 依存関係ツリー概要

```mermaid
graph TD
    MainLayout[MainLayout.tsx] -->|projectPath / refreshKey| GitGraph[GitGraph.tsx]
    GitGraph -->|Render| DOM[containerRef.current]
    GitGraph -->|Call| getTemplate[getTemplate]
    getTemplate -->|Use| templateExtend[@gitgraph/js]
```
