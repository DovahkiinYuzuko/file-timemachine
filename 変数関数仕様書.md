# 変数関数仕様書

## フロントエンド (React)

### コンポーネント

#### `App`
- **ファイル**: `src/App.tsx`
- **説明**: アプリケーションのエントリポイント。全画面レイアウトを管理。セットアップの完了状態に応じてウィザードまたはメインレイアウトを表示する。
- **状態 (State)**:
  - `setupCompleted: boolean | null`: セットアップが完了しているかどうか。`localStorage` (key: `setup_completed`) で永続化される。初期値は `null` で、ロード後に判定される。
- **依存関係**:
  - `MainLayout` (src/components/layout/MainLayout.tsx)
  - `Wizard` (src/components/setup/Wizard.tsx)
  - `src/utils/logger` (デバッグログ用)
  - `src/i18n/config` (初期化用インポート)
- **影響範囲**: 全体

#### `MainLayout`
- **ファイル**: `src/components/layout/MainLayout.tsx`
- **説明**: サイドバー付きの3カラムレイアウトを提供。`activeTab` に応じて表示するパネルを切り替える。
- **状態 (State)**:
  - `activeTab: SidebarTab`: サイドバーで現在選択されているタブ。表示パネルの制御に使用される。
  - `projectPath: string | null`: 現在開いているプロジェクト（フォルダ）のフルパス。
  - `selectedFilePath: string | null`: 現在プレビュー対象として選択されているファイルのフルパス。
  - `isSettingsModalOpen: boolean`: 設定モーダルの表示状態。
  - `isHelpModalOpen: boolean`: ヘルプモーダルの表示状態。
  - `safetyIssues: SafetyIssue[]`: 安全スキャンで見つかった問題のリスト。
- **依存関係**:
  - `Sidebar` (src/components/layout/Sidebar.tsx)
  - `SettingsModal` (src/components/settings/SettingsModal.tsx)
  - `HelpModal` (src/components/help/HelpModal.tsx)
  - `FileTree` (src/components/tree/FileTree.tsx)
  - `FilePreview` (src/components/preview/FilePreview.tsx)
  - `SafetyDialog` (src/components/guard/SafetyDialog.tsx)
  - `src/utils/logger` (アクションの記録用)
  - `src/utils/safety` (安全スキャンロジック)
  - `react-resizable-panels` (外部ライブラリ)
- **影響範囲**: アプリケーションの基本レイアウト。表示の出し分け。プロジェクトパスおよび選択ファイルの管理。

#### `Sidebar`
- **ファイル**: `src/components/layout/Sidebar.tsx`
- **説明**: アプリケーションの左端に固定表示されるスリムなナビゲーションバー。機能の切り替えとフォルダ選択を行う。
- **プロパティ**:
  - `activeTab: SidebarTab`: 現在アクティブなタブのID。
  - `onTabChange: (tab: SidebarTab) => void`: タブ切り替え時のコールバック。
  - `onOpenFolder: (path: string) => void`: フォルダ選択時のコールバック。
- **型定義**:
  - `SidebarTab`: `"files" | "history" | "settings" | "help"`
- **依存関係**:
  - `src/utils/logger` (操作ログ用)
  - `lucide-react` (アイコン)
  - `@tauri-apps/plugin-dialog` (フォルダ選択ダイアログ)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: アプリケーション全体のナビゲーション。フォルダ選択エントリ。

#### `SettingsModal`
- **ファイル**: `src/components/settings/SettingsModal.tsx`
- **説明**: 拡張設定画面。言語選択、GitHub連携、アプリの挙動、セキュリティ設定を行う。設定値は `localStorage` に保存される。言語切り替えは即時反映される。
- **プロパティ**:
  - `isOpen: boolean`: モーダルの表示状態。
  - `onClose: () => void`: モーダルを閉じる際のコールバック。
- **状態 (State)**:
  - `saveBehavior: SaveBehavior`: 保存時の挙動設定。`localStorage` (key: `settings_save_behavior`) で永続化。
  - `autoScan: boolean`: 自動脆弱性スキャンの有効/無効。`localStorage` (key: `settings_auto_scan`) で永続化。
  - `theme: Theme`: アプリのテーマ（"light" | "dark"）。`localStorage` (key: `settings_theme`) で永続化。
- **依存関係**:
  - `src/utils/logger` (設定変更ログ用)
  - `src/i18n/config` (言語切り替え用)
  - `@tauri-apps/plugin-shell` (ブラウザ起動用)
  - `react-i18next` (`useTranslation`, `i18n` インスタンス)
- **影響範囲**: アプリケーションの設定管理。永続化。

#### `HelpModal`
- **ファイル**: `src/components/help/HelpModal.tsx`
- **説明**: Gitコマンド早見表を表示するヘルプ画面。アプリの操作とGitコマンドの対応を解説する。
- **プロパティ**:
  - `isOpen: boolean`: モーダルの表示状態。
  - `onClose: () => void`: モーダルを閉じる際のコールバック。
- **依存関係**:
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: アプリケーションのヘルプ機能。

#### `Tooltip`
- **ファイル**: `src/components/common/Tooltip.tsx`
- **説明**: ホバー/フォーカス時に補足説明を表示する汎用ツールチップ。
- **プロパティ**:
  - `content: string`: 表示する説明文。
- **依存関係**:
  - `lucide-react` (アイコン)
- **影響範囲**: ユーザーインターフェース各所の解説。

#### `GitGraph`
- **ファイル**: `src/components/graph/GitGraph.tsx`
- **説明**: `@gitgraph/react` を使用して Git のコミット履歴を可視化する。
- **プロパティ**: なし
- **依存関係**:
  - `@gitgraph/react` (外部ライブラリ)
  - `@tauri-apps/api/core` (`invoke`)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: ルート管理パネル

#### `FilePreview`
- **ファイル**: `src/components/preview/FilePreview.tsx`
- **説明**: 選択されたファイルの内容（テキスト、画像）を実際に読み取って表示する。
- **プロパティ**:
  - `filePath: string | null`: 表示するファイルのフルパス。変更されると自動で中身を再読み込みする。
- **状態 (State)**:
  - `content: FileContent | null`: 読み取ったファイルデータ（テキストまたはBase64画像）。
  - `isLoading: boolean`: 読み込み中フラグ。
  - `error: string | null`: 読み込みエラーメッセージ。
- **依存関係**:
  - `invoke` (@tauri-apps/api/core)
  - `src/utils/logger` (読み込み状況の記録)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: プレビューパネルの内容表示。

#### `FileTree`
- **ファイル**: `src/components/tree/FileTree.tsx`
- **説明**: プロジェクトのディレクトリ構造をツリー形式で表示する。フォルダの開閉やファイルの選択、Git無視設定のトグルが可能。
- **プロパティ**:
  - `rootPath: string | null`: 走査対象のルートパス
  - `onFileSelect?: (path: string) => void`: ファイル選択時のコールバック
- **状態 (State)**:
  - `treeData: FileEntry[]`: 取得したファイルツリーのデータ
  - `isLoading: boolean`: 読み込み中フラグ
  - `error: string | null`: エラーメッセージ
  - `gitMode: GitMode`: 現在選択されている管理モード（"whitelist" | "blacklist"）
- **依存関係**:
  - `src/utils/logger` (ツリー取得状況の記録)
  - `FileTreeItem` (同ファイル内、React.memo化)
  - `invoke` (@tauri-apps/api/core)
  - `lucide-react` (Folder, FileText, ChevronRight, ChevronDown, Loader2)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: サイドバーのファイル一覧表示。Git管理モードの切り替え。

#### `FileTreeItem`
- **ファイル**: `src/components/tree/FileTree.tsx`
- **説明**: ツリーの各要素を描画する再帰コンポーネント。チェックボックスによるGit無視設定の操作をサポート。
- **プロパティ**:
  - `entry: FileEntry`: 描画対象のエントリ情報
  - `depth: number`: 階層の深さ（インデント用）
  - `gitMode: GitMode`: 現在の管理モード
  - `onFileSelect?: (path: string) => void`: ファイル選択時のコールバック
  - `onToggleIgnore: (path: string, currentIgnored: boolean) => void`: 無視設定切り替え時のコールバック
- **状態 (State)**:
  - `isOpen: boolean`: フォルダの開閉状態
- **依存関係**:
  - `lucide-react` (アイコン)
- **影響範囲**: ファイルツリーの個別の項目。

#### `Wizard`
- **ファイル**: `src/components/setup/Wizard.tsx`
- **説明**: 環境診断ウィザード。Git, Homebrew, gh のインストール状況を Rust 側から取得し、結果を表示する。不足している場合はインストールガイドへのリンクを提供する。
- **プロパティ**:
  - `onComplete: () => void`: セットアップ完了時に呼び出されるコールバック。
- **依存関係**:
  - `src/utils/logger` (診断状況の記録)
  - `@tauri-apps/api/core` (`invoke`)
  - `lucide-react` (アイコン)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: アプリケーション起動時のセットアッププロセス

#### `SafetyDialog`
- **ファイル**: `src/components/guard/SafetyDialog.tsx`
- **説明**: 危険なファイルや巨大なファイルが検知された際に警告を表示するダイアログ。除外して保存、強制保存、キャンセルのアクションを提供。
- **プロパティ**:
  - `isOpen: boolean`: ダイアログの表示状態
  - `issues: SafetyIssue[]`: 検知された問題のリスト
  - `onClose: () => void`: キャンセル時のコールバック
  - `onConfirmAnyway: () => void`: 強制保存時のコールバック
  - `onConfirmExclude: () => void`: 除外保存時のコールバック
- **依存関係**:
  - `src/utils/safety` (型定義: `SafetyIssue`)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲**: 保存時の確認プロセス

### その他 (ユーティリティ・設定)

#### `safety (analyzeFilesForSafety)`
- **ファイル**: `src/utils/safety.ts`
- **説明**: ファイルのリストを解析し、特定の拡張子（.env, .pem など）や巨大ファイル（100MB超）を検知する。
- **引数**:
  - `files: FileInfo[]`: 解析対象のファイルリスト（パスとサイズ）
- **戻り値**: `SafetyIssue[]`: 検知された問題（パス、理由、深刻度）のリスト
- **依存関係**: なし
- **影響範囲**: 保存時の安全チェック

#### `logger`
- **ファイル**: `src/utils/logger.ts`
- **説明**: `tauri-plugin-log` をラップしたロガーユーティリティ。Rust側とWebview側のログを統合し、ブラウザのコンソールにも出力可能。
- **メソッド**:
  - `info(message: string)`
  - `warn(message: string)`
  - `error(message: string)`
  - `debug(message: string)`
  - `trace(message: string)`
  - `attachConsole(): Promise<UnlistenFn>`
- **依存関係**:
  - `@tauri-apps/plugin-log` (外部ライブラリ)
- **影響範囲**: アプリケーション全体のデバッグ・トレーサビリティ

#### `i18n (config)`
- **ファイル**: `src/i18n/config.ts`
- **説明**: `i18next` の初期化設定。16言語（ja, en, zh-CN, zh-TW, ko, th, vi, id, es, fr, de, pt, it, ru, ar, hi）をサポート。
- **機能**:
  - `supportedLngs`: サポート言語リストの定義。
  - `languageChanged` イベント: 言語切り替え時に HTML の `dir` 属性（RTL対応）と `lang` 属性を自動更新する。
- **依存関係**: `i18next`, `react-i18next`, `i18next-http-backend`, `i18next-browser-languagedetector`
- **影響範囲**: アプリケーション全体の多言語対応

#### `@gitgraph/react (型定義)`
- **ファイル**: `src/types/gitgraph.d.ts`
- **説明**: `@gitgraph/react` ライブラリのアンビエント型宣言。
- **依存関係**: なし
- **影響範囲**: TypeScript コンパイル

## バックエンド (Rust/Tauri)

### 関数

#### `greet`
- **ファイル**: `src-tauri/src/lib.rs` (Tauri v2)
- **説明**: デフォルトの挨拶関数。初期テンプレート用。
- **引数**:
  - `name: &str`
- **戻り値**: `String`
- **依存関係**: なし
- **影響範囲**: 動作確認用。

#### `check_dependencies`
- **ファイル**: `src-tauri/src/commands/setup.rs`
- **説明**: システムに Git, Homebrew, gh がインストールされているかチェックする。
- **引数**: なし
- **戻り値**: `Result<DependencyStatus, String>`
- **依存関係**:
  - `std::process::Command`
- **影響範囲**: 環境診断ウィザード

#### `git_init`
- **ファイル**: `src-tauri/src/commands/git.rs`
- **説明**: 指定されたパスを正規化し、`.git` が存在しない場合のみ `git init` を実行する。既にある場合は初期化をスキップ。必要に応じて `.gitignore` を生成。ログを `log` で出力。
- **引数**:
  - `path: String`: 初期化するディレクトリのパス
- **戻り値**: `Result<String, String>`
- **依存関係**:
  - `std::process::Command`, `std::fs`, `std::path::Path`, `dunce`, `log`
- **影響範囲**: リポジトリの初期設定。既存リポジトリへのアタッチ。

#### `git_commit`
- **ファイル**: `src-tauri/src/commands/git.rs`
- **説明**: 指定されたパスを正規化し、リポジトリ内で `git add .` と `git commit` を実行する。ログを `log` で出力。
- **引数**:
  - `path: String`: リポジトリのパス
  - `message: String`: コミットメッセージ
- **戻り値**: `Result<String, String>`
- **依存関係**:
  - `std::process::Command`, `std::path::Path`, `dunce`, `log`
- **影響範囲**: 履歴の保存機能。

#### `git_log`
- **ファイル**: `src-tauri/src/commands/git.rs`
- **説明**: 指定されたパスを正規化し、リポジトリのコミットログをパースして取得する。ログを `log` で出力。
- **引数**:
  - `path: String`: リポジトリのパス
- **戻り値**: `Result<Vec<CommitLog>, String>`
- **依存関係**:
  - `std::process::Command`, `std::path::Path`, `dunce`, `log`
- **影響範囲**: 履歴表示機能。

#### `get_file_tree`
- **ファイル**: `src-tauri/src/commands/files.rs`
- **説明**: 指定されたパス配下を再帰的に走査し、階層構造（ツリー形式）で返す。`.git`, `node_modules`, `target` は除外。`git check-ignore` を使用して無視状態（is_ignored）を判定。パスの正規化に `dunce` を使用。
- **引数**:
  - `root_path: String`: 走査を開始するルートディレクトリのパス
- **戻り値**: `Result<Vec<FileEntry>, String>`
- **依存関係**:
  - `walkdir`, `dunce`, `log`, `serde`, `std::process::Command`
- **影響範囲**: ファイルツリー表示機能

#### `update_gitignore`
- **ファイル**: `src-tauri/src/commands/git.rs`
- **説明**: `.gitignore` ファイルを更新し、指定されたパスの無視設定を変更する。管理モード（whitelist/blacklist）に応じて追記/削除を適切に処理する。
- **引数**:
  - `repo_path: String`: リポジトリのルートパス
  - `target_path: String`: 対象ファイルの相対パス
  - `mode: String`: 管理モード
  - `to_ignore: bool`: 無視するかどうかのフラグ
- **戻り値**: `Result<(), String>`
- **依存関係**:
  - `std::fs`, `std::path::Path`, `dunce`, `log`
- **影響範囲**: ファイルツリーのチェックボックス操作

#### `get_project_config`
- **ファイル**: `src-tauri/src/commands/config.rs`
- **説明**: プロジェクトの設定（.file-timemachine.json）を取得する。
- **引数**:
  - `root_path: String`: プロジェクトのルートパス
- **戻り値**: `Result<ProjectConfig, String>`
- **依存関係**: `serde`, `std::fs`, `std::path::PathBuf`, `log`
- **影響範囲**: ファイルツリーの管理モード切り替え

#### `set_project_config`
- **ファイル**: `src-tauri/src/commands/config.rs`
- **説明**: プロジェクトの設定を保存する。
- **引数**:
  - `root_path: String`: プロジェクトのルートパス
  - `config: ProjectConfig`: 保存する設定値
- **戻り値**: `Result<(), String>`
- **依存関係**: `serde`, `std::fs`, `std::path::PathBuf`, `dunce`, `log`
- **影響範囲**: プロジェクト単位の設定変更

#### `read_file_content`
- **ファイル**: `src-tauri/src/commands/preview.rs`
- **説明**: 指定されたパスのファイルを読み取り、テキストまたはBase64エンコードされた画像データを返す。5MBを超えるファイルは安全のため制限する。
- **引数**:
  - `path: String`: 読み取り対象のフルパス
- **戻り値**: `Result<FileContent, String>`
  - `FileContent` 構造体 (content_type: String, data: String)
- **依存関係**:
  - `std::fs`, `std::path::Path`, `base64`, `dunce`, `log`
- **影響範囲**: ファイルプレビュー機能


### プラグイン

#### `tauri-plugin-log`
- **ファイル**: `src-tauri/src/lib.rs` (初期化)
- **説明**: ログの統合管理。Stdout, LogDir, Webview への出力。
- **依存関係**:
  - `tauri-plugin-log` (外部ライブラリ)
- **影響範囲**: システム全体

#### `tauri-plugin-shell`
- **ファイル**: `src-tauri/src/lib.rs` (初期化)
- **説明**: システムシェル操作（ブラウザオープン等）を可能にする。
- **依存関係**:
  - `tauri-plugin-shell` (外部ライブラリ)
- **影響範囲**: 設定画面（GitHub OAuth 連携など）

#### `tauri-plugin-dialog`
- **ファイル**: `src-tauri/src/lib.rs` (初期化)
- **説明**: システム標準のダイアログ（フォルダ選択等）を表示する。
- **依存関係**:
  - `tauri-plugin-dialog` (外部ライブラリ)
- **影響範囲**: フォルダ選択機能
