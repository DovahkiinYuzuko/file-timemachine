# 変数関数仕様書

このドキュメントは、プロジェクト内の重要な変数、関数、コンポーネントの仕様と依存関係を管理するためのものです。

---

## 1. コンポーネントおよび関数定義

### `GitGraph` (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx`
- **概要**: Gitのコミットログを取得し、路線図またはツリーとして可視化するReactコンポーネント。
- **Props**:
  - `projectPath: string | null` - 可視化対象のリポジトリパス。
  - `refreshKey?: number` - 更新をトリガーするためのキー値。
  - `onInitSuccess?: () => void` - リポジトリ初期化成功時のコールバック。

---

## 2. 変更・追加された関数仕様 (2026-05-20 ハイブリッド化更新)

### `getTemplate` (Internal Function)
- **概要**: `@gitgraph/js` のレンダリングテンプレートを生成するヘルパー関数。ハイブリッドレイアウトに対応し、ツリーと路線図で明確なグラフィック差別化を行います。

#### 内部変更点 (Hybrid Layout & Premium Design)
- **SVGテキストの排除**: `commit.message.display: false` および `branch.label.display: false` に設定し、SVG内から文字描画を完全にオフにしました。
- **デザインの完全差別化**: 
  - `style === "tree"` 時は、`lineWidth: 2`、`dotSize: 4` の極細・極小でシックなモノトーンに設定。
  - `style === "metro"` 時は、`lineWidth: 4`、`dotSize: 6` の太め・ポップなCUDカラフル配色に設定。
- **Spacingの固定化**: SVG内のコミット間隔を `Math.round(40 * currentScale)` に固定し、右側のHTMLリストの行高と完璧に同期するようにしました。

### コンポーネント構造 (JSX) の変更
- **概要**: `@gitgraph/js` によるSVGレンダリング領域と、コミットテキストを描画するHTML（React）領域を横並びに分離するハイブリッド構造に変更しました。
- **追加されたクラス**:
  - `.git-graph-body`: 全体を囲むフレックスコンテナ（横並び）。
  - `.git-graph-commit-list`: 右側に配置されるHTMLのコミットリスト。
  - `.commit-list-row`: 各コミットの行。SVGの `spacing` と連動して高さ（`height`）が設定されます。
  - `.commit-branch-badge`: `main` 等のブランチ名をHTML上で描画するためのピル型バッジ。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `commits` (状態配列 / 各コミット情報をHTMLリストとしてループ描画するために使用)
  - `scale` (状態 / HTML側の行高を同期するために `Math.round(40 * scale)` として利用)
- **影響範囲 (Impact Scope)**:
  - `GitGraph.tsx` のレンダーツリー全体。
  - スクロールおよびレイアウト制御（`GitGraph.css` の全面的なフレックス化により、グラフとリストが同期してスクロールします）。

---

## 3. 変更・追加された関数仕様 (Canvas Zoom/Pan & AppConfig 更新)

### Rustバックエンド: `app_config.rs`
- **概要**: `localStorage` を完全に廃止し、OSの標準設定ディレクトリに `app_config.json` を保存してネイティブに設定を管理するためのTauriコマンド。
- **定義**: 
  - `get_app_config(app: tauri::AppHandle) -> Result<AppConfig, String>`
  - `set_app_config(app: tauri::AppHandle, config: AppConfig) -> Result<(), String>`
- **依存関係**: `serde_json`, `tauri::Manager`
- **影響範囲**: アプリ全体の設定データ永続化。

### TypeScript API: `api/config.ts`
- **概要**: Rustの `app_config` コマンドを非同期で呼び出すためのラッパー関数群。
- **定義**:
  - `getAppConfig()`, `setAppConfig(config)`
  - `updateAppConfig(partialConfig)`
- **影響範囲**: `App.tsx`, `MainLayout.tsx`, `SettingsModal.tsx` における `localStorage` の直接操作をすべて置き換え。

### `GitGraph` の無限キャンバス化 (Canvas Zoom/Pan)
- **概要**: CSSの `transform: translate(x,y) scale(z)` を活用したドラッグ移動（パン）とホイール拡大縮小（ズーム）の実装。
- **追加状態**:
  - `offset: { x: number, y: number }` - キャンバスの移動量。
  - `isDragging: boolean` - ドラッグ状態の判定。
- **依存関係マッピング**:
  - **依存元**: `scale` と `offset` を使って、内包する `.git-graph-canvas` 全体の `transform` を動的に更新。
  - **影響範囲**: `.git-graph-viewport` のマウスイベントハンドラ(`onMouseDown`, `onMouseMove`, `onWheel` など) によって制御。`getTemplate` で行っていた疑似的なスケール調整は排除され、常に等倍で描画したものをCSSで変形する方式に変更されました。

---

## 4. 依存関係ツリー概要

```mermaid
graph TD
    MainLayout[MainLayout.tsx] -->|projectPath / refreshKey| GitGraph[GitGraph.tsx]
    MainLayout -->|Call| updateAppConfig[api/config.ts]
    GitGraph -->|Render SVG| DOM[containerRef.current]
    GitGraph -->|Render HTML| CommitList[.git-graph-commit-list]
    GitGraph -->|Apply Transform| Canvas[.git-graph-canvas]
    GitGraph -->|Call| getTemplate[getTemplate]
    getTemplate -->|Use| templateExtend[@gitgraph/js]
    updateAppConfig -->|Invoke| RustAppConfig[app_config.rs]
```

---

## 5. 変更・追加された関数仕様 (2026-05-20 gitignore連携とパス正規化バグ修正)

### Rustバックエンド: `commands/git.rs` (`update_gitignore`)
- **概要**: フロントエンドのチェックボックス操作に応じて `.gitignore` を更新するコマンド。
- **変更点**: 
  - `is_dir` 引数を追加し、ディレクトリの場合は `dir/` と `dir/**` の2パターンの除外/除外解除ルールを同時に生成して追加・削除するように拡張。これによりフォルダ操作時に子ファイル群にも正しく状態が波及するようになりました。
  - Windows環境における「パスの表記ゆれ（大文字小文字の違いやUNCパス化など）」を防ぐため、`target_path` を `dunce::canonicalize` によって正規化するように修正。
- **依存関係**: 影響元の `FileTree` から `isDir` パラメータを受け取ります。

### Rustバックエンド: `commands/files.rs` (`get_file_tree`)
- **概要**: 指定ディレクトリ内のファイルツリーを構築し、各ファイルの `is_ignored` 状態を判定するコマンド。
- **変更点**: 
  - `git check-ignore` が非ASCII文字（日本語等）のファイル名をダブルクォート＋オクタルエスケープ形式（例：`"\343\203..."`）で返す仕様に対応するため、ヘルパー関数 `parse_git_ignore_output_line` および `unescape_git_path` を実装。エスケープ解除後のUnicode文字列で `ignored_set` と照合することで、日本語名ファイルの無視状態バグを完全に解決しました。
  - パスのプレフィックス削除 (`strip_prefix`) 時に正規化済みのルートパスを使用するように修正。
- **依存関係**: 依存する関数として `parse_git_ignore_output_line`, `unescape_git_path` を同一ファイル内に新規追加。

### TypeScriptコンポーネント: `FileTree.tsx`
- **概要**: ファイルツリーを描画・管理するReactコンポーネント。
- **追加関数**:
  - `updateIgnoreState(entries: FileEntry[], targetPath: string, newIsIgnored: boolean): FileEntry[]`
    - ツリーデータ内の特定パスのエントリの `is_ignored` を再帰的に更新する純粋なヘルパー関数。
- **変更点 (Optimistic UI)**:
  - `handleToggleIgnore` において、Rustへのバックエンドコールバック（`update_gitignore`）を待たずに即座に `setTreeData` 経由で `updateIgnoreState` を実行し、UI上のチェックボックス切り替えを瞬時に反映する「楽観的更新」を実装。
  - バックグラウンドで同期更新し、通信エラー時は以前の状態へロールバックする堅牢なエラーハンドリングを追加。これによりチェックごとのローディング画面が排除され、UXが劇的に向上しました。
  - `fetchTree` に `silent` フラグを導入し、バックグラウンドでの再同期時はローディング表示を行わないようにしました。

### 翻訳リソース: `translation.json` (`git_mode`)
- **概要**: ユーザーにより直感的な操作感を提供するため、「厳選」や「おまかせ」という曖昧な表現を排し、より説明的な文言に変更。
- **変更点**:
  - `whitelist`: "厳選" ➔ "チェックしたファイルだけ保存"
  - `blacklist`: "おまかせ" ➔ "チェックしたファイル以外を全部保存"

---

## 6. 変更・追加された関数仕様 (2026-05-21 お試しルート・リアルタイムプレビュー更新)

### Rustバックエンド: `commands/git.rs` (ブランチ・ルート管理)
- **概要**: ブランチ（ルート）の取得、作成、切り替えに関するコマンドを追加。
- **追加関数**:
  - `git_get_current_branch(path: String) -> Result<String, String>`
    - 現在のブランチ名を取得 (`git branch --show-current`)。
  - `git_create_branch(path: String, branch_name: String) -> Result<String, String>`
    - 新しいブランチを作成し、そこに切り替える (`git checkout -b <branch_name>`)。
  - `git_get_branches(path: String) -> Result<Vec<String>, String>`
    - ローカルのブランチ一覧を取得 (`git branch --format="%(refname:short)"`)。
  - `git_checkout(path: String, branch: String) -> Result<String, String>`
    - 指定したブランチに切り替える (`git checkout <branch>`)。

### フロントエンド (React): `SwitchBranchModal` & `CreateBranchModal`
- **概要**: ユーザーがGUIから直感的にルートを作成・切り替えできるモーダルコンポーネント群。
- **依存関係**: `MainLayout.tsx` から呼び出され、フッターの「ルートボタン」と連携します。
- **影響範囲**: ルート作成/切り替えが成功すると、`historyRefreshKey` が更新され、`GitGraph` と `FileTree` が再レンダリングされて最新のブランチ状態に同期します。

### フロントエンド (React): `FilePreview.tsx` (リアルタイムプレビュー)
- **概要**: バックエンドの大がかりなファイル監視（fs-watch等）を使用せず、表示中ファイルのメタデータ（最終更新日時）を1秒間隔でポーリングチェックすることで軽量なリアルタイム更新を実現。
- **変更点**:
  - `useEffect` 内部に `setInterval` を追加し、`get_file_info` を毎秒呼び出し。
  - `useRef` で保持している前回の `modified` と比較し、変更があれば `read_file_content` をサイレント（ローディング非表示）で再フェッチして表示を更新。
- **依存関係**: 選択中の `filePath` にのみ依存。別のファイルが選択されるとインターバルがクリーンアップ・再設定されます。

---

## 7. 変更・追加された関数仕様 (2026-05-21 検索＆差分(Diff)機能・システムメッセージのフォーマル化)

### Rustバックエンド: `commands/git.rs` (差分取得)
- **概要**: 選択したファイルの未コミットの変更分（差分）を取得するコマンド。
- **追加関数**:
  - `git_diff_file(path: String) -> Result<String, String>`
    - `git diff HEAD -- <file>` を実行し、テキスト形式の差分を返却します。ディレクトリや未初期化リポジトリの場合は空文字を返します。

### フロントエンド (React): 検索機能 (`FileTree.tsx`, `GitGraph.tsx`)
- **概要**: ファイル一覧とコミット履歴から目的のものを素早く見つけるためのローカルフィルタリング機能。
- **追加状態とロジック**:
  - `FileTree.tsx`:
    - `searchQuery` (状態): 検索キーワード。
    - `filterTree(nodes, query)`: 再帰的に `treeData` をフィルタリング。マッチしたファイルとその親ディレクトリのみを残します。
    - 検索実行時は `FileTreeItem` の `forceExpand` propを true にし、該当フォルダを自動展開します。
  - `GitGraph.tsx`:
    - `searchQuery` (状態): 検索キーワード。
    - コミットの `message` や `hash` と部分一致させ、一致しない `.commit-list-row` の `opacity` を下げて半透明にすることで該当コミットを視覚的にハイライトします。

### フロントエンド (React): 差分表示機能 (`FilePreview.tsx`)
- **概要**: ファイルプレビュー画面で、最後のコミットからの変更箇所（Diff）を赤と緑でハイライト表示する機能。
- **追加状態とロジック**:
  - `showDiff` (状態): 差分モードのON/OFFトグル。
  - `diffContent` (状態): Rustから取得した差分テキスト。
  - ポーリング更新時やトグル時に `git_diff_file` を呼び出し、差分テキストを行ごとに分割して `+` は緑、`-` は赤、`@@` は青の背景色で装飾してレンダリングします。

### システムメッセージのフォーマル化
- 概要: `MainLayout.tsx`, `CreateBranchModal.tsx`, `SwitchBranchModal.tsx` 内の `alert` や `logger`、エラーメッセージのテキストを、ユーザーの指定通りフォーマルな日本語表現（「〜です・ます」調など）に修正しました。

---

## 8. 変更・追加された関数仕様 (2026-05-21 本番に採用（Merge）機能)

### Rustバックエンド: `commands/git.rs` (マージ・競合解決)
- **概要**: お試しルート（ブランチ）の内容を本番（main）に合流させ、競合が発生した場合の解決を支援するコマンド群。
- **追加関数**:
  - `git_merge_to_main(path: String, branch: String) -> Result<String, String>`
    - `main` ブランチへ切り替え後、指定ブランチを `git merge`。競合時は `"CONFLICT"` をエラーとして返却。
  - `git_get_conflicts(path: String) -> Result<Vec<String>, String>`
    - `git diff --name-only --diff-filter=U` により、競合が発生しているファイルパスのリストを取得。
  - `git_resolve_conflict(path: String, file: String, resolution: String) -> Result<(), String>`
    - `resolution` ("current" or "main") に基づき、`git checkout --theirs/--ours` を実行して競合を解決し、`git add` する。

#### 依存関係マッピング
- **依存元**: `MainLayout.tsx` からのマージ実行、および `ConflictResolverModal.tsx` からの解決操作。
- **影響範囲**: 成功すると `main` ブランチの履歴が更新され、UI上の `GitGraph` や `HistoryList` に反映されます。

---

## 9. 変更・追加された関数仕様 (2026-05-21 競合プレビュー拡張と多言語・堅牢化対応)

### Rustバックエンド: `commands/git.rs` (例外処理と多言語対応)
- **追加関数**:
  - `git_merge_abort(path: String, original_branch: String) -> Result<String, String>`
    - 競合解決を中止した際、`git merge --abort` を実行し、安全に元のブランチへ復帰させるコマンド。
- **変更点**:
  - `git_init` 時、Gitの `core.quotepath` を `false` に設定するように修正。これにより、日本語等の多言語ファイル名がエスケープされず、OS間でのパス互換性が向上。
  - すべてのコマンドの戻り値を「生テキストのメッセージ」から、i18nキー（例: `error.failed_to_canonicalize_path`）に変更。

### Rustバックエンド: `commands/preview.rs` (`read_file_content`)
- **変更点**:
  - **バイナリ検出**: ファイルの先頭 1024 バイトをスキャンし、nullバイトが含まれる場合に `is_binary: true` を返すロジックを実装。
  - **多言語対応**: `chardetng` による高精度な文字コード判別を維持しつつ、エラー発生時は i18n キーを返却するように刷新。
  - **PathBuf結合**: 引数の `path` に対して `dunce::canonicalize` と `PathBuf::join` を徹底し、OSに依存しない絶対パス解決を実現。

### フロントエンド (React): `ConflictResolverModal.tsx` (2ペインプレビュー)
- **概要**: 競合ファイルをリスト表示するだけでなく、選択したファイルの内容（競合マーカー含む）をリアルタイムにプレビューできるリッチなUI。
- **追加状態とロジック**:
  - `selectedFile`, `previewContent`, `previewLoading` (状態): プレビュー制御用。
  - `getLineClass(line)`: プレビュー行が `<<<<<<<`, `=======`, `>>>>>>>` 等の競合マーカーかどうかを判別し、ハイライト用のCSSクラスを割り当てる。
  - `renderPreviewContent()`: テキスト、バイナリ、読み込み中、エラー（翻訳適用済）の各状態を出し分けるレンダリングロジック。
- **依存関係**: `MainLayout.tsx` の `onCancel` ハンドラから `git_merge_abort` を呼び出し、モーダルを閉じる際のロールバックを保証します。
- **多言語化**: `t(errorCode)` により、Rustから返ってきたエラー識別子をフロントエンドで即座に翻訳。


---

## 10. 変更・追加された仕様 (2026-05-21 多言語化の完全解消とUIテキスト of フォーマル化)

### 翻訳リソースと多言語対応の強化 (`translation.json` / `useTranslation`)
- **概要**: アプリケーション内のすべてのハードコードされた日本語テキスト（警告、確認メッセージ、モーダルタイトル、プレースホルダー、スクリーンリーダー向け `aria-label`、ボタンテキストなど）を、i18nextフレームワークを使用した動的な多言語対応（`t()` 関数）に移行しました。また、UI上で表示される日本語のトーンを「です・ます調（フォーマルな表現）」に統一しました。
- **追加された翻訳キー**:
  - `common.action.retry`, `common.action.close`, `common.action.cancel`, `common.aria.show_help`, `common.empty_directory`
  - `conflict.no_conflicts`, `conflict.commit_message`
  - `branch.modal` (title, desc, input_label, placeholder, input_tip, submit, error_empty, error_invalid)
  - `branch.switch` (title, desc, error_fetch)
  - `graph.empty` (no_folder_title, no_folder_desc, no_history_title, no_history_desc)
  - `graph.init` (loading, start)
  - `graph.search.placeholder`
  - `graph.action` (zoom_out, zoom_in, reset_camera)
  - `history.table` (message, hash)
  - `layout.alert` (no_folder_save, save_success, save_failed, create_branch_success, create_branch_failed, switch_branch_success, switch_branch_failed, merge_success, merge_failed, save_local_only, no_folder_to_save, resolve_conflict_success, abort_merge_failed)
  - `layout.confirm.merge`
  - `layout.tooltip` (switch_route, merge_to_main, create_route)
  - `layout.action` (new_route, saving)
  - `preview.action` (back_to_preview, show_diff)
  - `preview.no_changes`, `preview.video_not_supported`, `preview.audio_not_supported`
  - `tree.action` (unignore, ignore)

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `translation.json` (16言語分の辞書ファイル。すべての新規追加キーが完全に定義・同期されていること)
  - `react-i18next` (`useTranslation` フック)
- **影響範囲 (Impact Scope)**:
  - `ConflictResolverModal.tsx` (競合解消メッセージ、コミットログメッセージ)
  - `CreateBranchModal.tsx` (バリデーションエラー、タイトル、プレースホルダー、ヘルプ文言、各種ボタン)
  - `SwitchBranchModal.tsx` (一覧取得エラーメッセージ、タイトル、説明、キャンセルボタン、閉じるボタンの aria-label)
  - `Tooltip.tsx` (スクリーンリーダー用の sr-only 内のラベル `common.aria.show_help`)
  - `GitGraph.tsx` (フォルダ未選択、履歴未検出、初期化状態、検索プレースホルダー、ズーム・リセットボタンの title)
  - `HistoryList.tsx` (テーブルヘッダーの Date/Message/Hash に対応する `common.last_modified`, `history.table.message`, `history.table.hash`)
  - `MainLayout.tsx` (保存確認・結果、ルート作成・切替・マージ確認・結果、アボート結果などの標準ブラウザ `alert` / `confirm` に渡すテキストすべて、フッターのボタン、ツールチップ、状態表示テキスト)
  - `FilePreview.tsx` (差分切り替えトグルの表示、差分検出結果、ビデオ/オーディオタグのフォールバックメッセージ)
  - `FileTree.tsx` (無視チェックボックスの `aria-label`、空ディレクトリ時の「ファイルが見つかりません」)

---

## 11. 変更・追加された仕様 (2026-05-21 GitGraphツリー廃止と落ち着いたプレミアムフッターボタン)

### GitGraph (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx` / `GitGraph.css`
- **変更点**:
  - `graphStyle` 状態および「ツリー/路線図」表示切り替えトグルUIを完全に廃止し、美しく整理された「路線図（Metroスタイル）」に一本化しました。
  - `getTemplate` 関数から `style` 引数を排除し、常にCUD（岡部・柴田パレット）準拠のカラフルなMetroスタイルが適用されるように修正。
- **影響範囲**: `GitGraph` コンポーネントおよびUIの簡素化。不要になった `.graph-style-toggle` や `.toggle-btn` のCSSスタイルを `GitGraph.css` からクリーンアップ。

### フッターアクションボタンのプレミアム刷新
- **ファイルパス**: `app-file-timemachine/src/components/layout/MainLayout.tsx` / `MainLayout.css`
- **変更点**:
  - **マージボタンのアイコン変更**: マージボタン（`merge-to-main-btn`）のアイコンを `lucide-react` の `Check` から、よりコンテキストを体現する `GitMerge` アイコンに変更しました。
  - **プレミアムなマージボタンスタイリング**: クラス `.merge-to-main-btn` に新規スタイルを適用。ギラギラしたネオンや派手なエフェクトを完全に排除し、職場で使える品格のある「大人のプレミアムグリーン（マージ成功を象徴）」として定義。
  - **ホバー・インタラクションの最適化**: マウスホバー時に微細な上昇（`translateY(-1px)`）と落ち着いたグリーンのドロップシャドウを付与し、滑らかに遷移（`transition`）するインタラクションを実装。
  - **フッター内ボタンの一貫性 (高さを36pxに統一)**:
    - フッター内の全ボタン（`branch-info-btn`（現在のルート）, `create-branch-btn`（新しいルート）, `merge-to-main-btn`（マージ）, `save-state-btn`（状態を保存））の `height` を `36px`（`box-sizing: border-box`）に統一。
    - 各ボタンの余白、ボーダー、角丸（`border-radius: 6px`）を揃え、隣接ボタンと完全に整列するプロ仕様の美しいフッターUIにリニューアル。
    - **ライト・ダークモード双方の最適化**:
      - 各ボタンで `[data-theme="dark"]` によるホバー明度とシャドウ濃度の微調整を徹底し、白背景（ライトモード）でも黒背景（ダークモード）でもプレミアムで落ち着いた見栄えを保証。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `App.css` で定義された CSS変数（`--accent-color`, `--success-color`, `--hover-bg`, `--border-color` 等）
  - `lucide-react`（`GitMerge` アイコン）
- **影響範囲 (Impact Scope)**:
  - `MainLayout.tsx` のフッターレンダリング部分。
  - アプリフッターの全体のビジュアル・レイアウト。

---

## 12. 変更・追加された仕様 (2026-05-21 GitGraphブランチ路線と名前のカラー同期化)

### GitGraph (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx` / `GitGraph.css`
- **追加された状態 (State)**:
  - `commitBranchMap: Map<string, { name: string; color: string }>`
    - 各コミットハッシュに対する「所属ブランチ名」および「割り当てられた路線色」の対応マッピングを記録するReact状態。
- **カラー追跡・同期ロジック**:
  - `gitgraph` のDOMレンダリングを制御する `useEffect` 内で、古い順にコミットをプロットする際、ブランチの新規生成タイミングをフックし、バリアフリー対応の CUD カラーパレット（`#56b4e9`（スカイブルー）、`#e69f00`（オレンジ）、`#009e73`（青緑）、`#cc79a7`（マゼンタピンク））から順に色を割り当てます。
  - **ブランチ数無限増加への安全設計**: カラー割り当て時に、ブランチ数が増加してもエラーにならないよう `cudColors[nextColorIndex++ % cudColors.length]` のように循環モジュロ演算（`%`）を適用。
  - ループ内でマージ、分岐、通常の直系コミットなど、すべてのコミットパターンで所属ブランチ名と色が正確に同期・蓄積されます。
  - 計算完了後、無限再レンダリングを防止するため、直前の `commitBranchMap` と差異が生じた場合のみ `setCommitBranchMap` を更新する保護ロジックを実装。
- **HTML UIの改善**:
  - **所属路線ドットインジケーターの追加**: コミット履歴（HTML）の各行（`.commit-info-wrapper`）の左端に、所属路線と同じ色を持つ直径8pxのドット（`.commit-branch-dot`）を追加。バッジがない中間コミットの所属ブランチも一目で判別可能に。
  - **ブランチ名バッジの動的カラー化**: HEAD位置のブランチ名バッジ（`.commit-branch-badge`）にインラインで `style` 属性を適用し、所属路線の色から透過背景色（`color + "20"` / 透過度約12%）、文字色（`color`）、透過ボーダー枠線（`color + "40"` / 透過度約25%）を動的に生成して適用。
  
#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `cudColors` (CUDバリアフリー配色パレット)
  - `commits` (履歴配列 / 描画ループと同期トリガーに使用)
- **影響範囲 (Impact Scope)**:
  - `GitGraph.tsx` の HTML 履歴リストのレンダリング部、および各バッジ、新規追加ドット（`.commit-branch-dot`）。
  - `GitGraph.css` のクラス構造。

### 翻訳リソース (`translation.json`) の微修正
- **ファイルパス**: `app-file-timemachine/public/locales/ja/translation.json`
- **変更点**:
  - 設定画面のテーマ変更オプション `settings.theme.light` の値を、ユーザーの要望に基づき「ライト (GitHubオススメ)」から「ライト」へシンプルに変更し、UI文言から余分な表記を排除。

### 設定保存ファイル (`app_config.json`) の並行書き込み競合バグの根本解決
- **関連ファイルパス**:
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx`
  - `app-file-timemachine/src/api/config.ts`
  - `app-file-timemachine/src-tauri/src/commands/app_config.rs`
- **追加された状態・変数**:
  - `SettingsModal.tsx` 内の `isLoaded: boolean` (設定ロード完了フラグ)
  - `config.ts` 内の `updateQueue: Promise<void>` (書き込み直列化キュー)
- **バグの根本原因と解決ロジック**:
  - **根本原因**: `SettingsModal` のマウント時、非同期ロード前にReactの初期値が設定保存 `useEffect` をトリガーし、設定がデフォルトで上書きされる問題がありました。さらに、複数の処理から `updateAppConfig` が同時に呼ばれると、「Read-Modify-Write」の過程で並行書き込み競合（Race Condition）が発生し、JSONファイル末尾に `}}` などの余分な文字が書き込まれてパースエラー（`trailing characters`）を引き起こし、起動時にアプリが白くなったりセットアップ無限ループに陥る致命的なバグになっていました。
  - **解決策1 (ガード付き一括保存)**: `SettingsModal.tsx` にて、非同期ロードが完了するまで保存処理をブロックする `isLoaded` ガードを追加し、保存処理を単一の `useEffect` に一括統合してReactマウント時の不要な上書きを防止しました。
  - **解決策2 (書き込み直列化キュー)**: `config.ts` の `updateAppConfig` に Promise キューを導入し、複数箇所から同時に設定更新要求が来ても必ず1つずつ順番に処理（直列化）されるようにし、ファイル書き込みの競合を完全にブロックしました。
  - **解決策3 (JSON破損時の自己修復機能)**: `app_config.rs` の設定読み込み時、もしJSONのパースに失敗した場合でもエラーでアプリを止めるのではなく、壊れたファイルを `.bak` に退避させてデフォルト設定を返す「自己修復機能」を実装しました。これにより、次回保存時にクリーンなJSONで上書きされる安全な設計になっています。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `updateQueue` (API設定保存の直列化管理)
  - `isLoaded` (SettingsModalのロード後ガード判定)
- **影響範囲 (Impact Scope)**:
  - アプリ全体の設定保存フロー (`updateAppConfig` 呼び出し箇所すべて)
  - Tauri側の設定ファイル読み込みフォールバック処理 (`get_app_config`)

---

---

## 13. 変更・追加された仕様 (2026-05-21 セットアップウィザード＆シミュレーションモード実装)

### Rustバックエンド: `commands/setup.rs`
- **概要**: 依存関係（Git, GitHub CLI, Homebrew等）のインストール状態を診断し、未インストールの場合はOSネイティブのパッケージマネージャを呼び出して自動インストールを実行するコマンド。UIデザインや挙動の検証ができるよう「シミュレーションモード（デバッグモード）」を備える。
- **追加/変更関数**:
  - `check_dependencies(simulate: Option<bool>) -> Result<DependencyStatus, String>`
    - 既存コマンドの引数に `simulate` を追加。`true` の場合、実際はインストールされていてもすべて `false` (未インストール) を返却し、未インストール状態をシミュレーションします。
  - `install_dependency(app: tauri::AppHandle, tool: String, simulate: bool) -> Result<(), String>`
    - 新規追加コマンド。引数 `tool` (`"git" | "gh"`) に応じて自動インストールを実行します。
    - **Windows**: `winget install --silent ...` を子プロセスで実行。
    - **macOS**: `xcode-select --install` または `brew install gh` を子プロセスで実行。
    - **シミュレーション**: 実際にはコマンドを実行せず、タイマー (`tokio::time::sleep`) を使って数秒間かけて架空の進捗ログを出力します。
    - **リアルタイムログ**: 子プロセスの `stdout` / `stderr` を `BufReader` で行ごとに読み取り、`tauri::AppHandle::emit` を使用してフロントエンドへ `install-log` イベントとしてリアルタイムにストリーミング配信します。

### フロントエンド (React): `Wizard.tsx`
- **概要**: アプリケーション初回起動時の環境診断と自動セットアップを行うウィザードUIコンポーネント。
- **追加状態とロジック**:
  - `isSimulated` (状態): 右上のトグルスイッチで切り替えられるデバッグモードのフラグ。切り替え時に再度 `check_dependencies` が呼び出されます。
  - `installingTool`, `installProgress`, `installLogs` (状態): インストールの進行状況とログメッセージを管理。
  - `handleInstall` (関数): `install_dependency` を呼び出すと同時に、Tauriの `listen("install-log")` を使用してバックエンドからのリアルタイムログを受信し、状態を更新します。
  - **Git必須化のロジック**: アプリケーションの進行条件を `allInstalled = status.git` に変更し、GitHub CLI を「(任意) / (Optional)」扱いとしました。Git さえインストールされていれば「完了してアプリを起動」ボタンが有効になります。
- **UI更新**:
  - **シミュレーショントグル**: 画面右上にデバッグ用のトグルボタンを配置。
  - **プログレスバー＆ターミナル風UI**: インストール実行中、CSSアニメーション付きのプログレスバーと、黒背景のターミナル風領域（`.wizard-terminal`）にリアルタイムで流れるログが表示されます。ログが追加されるたびに一番下へ自動スクロール (`logsEndRef.scrollIntoView`) します。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `tauri::AppHandle::emit` (Rust -> React イベント通知)
  - `@tauri-apps/api/event` (React側のイベントリスナー `listen`)
  - `translation.json` (追加された `setup.optional`, `setup.install_btn`, `setup.installing_status`, `setup.simulation_mode` 等のキー)
  - `tokio` (Rustの非同期ランタイム / `time::sleep` によるシミュレーション遅延用)
- **影響範囲 (Impact Scope)**:
  - 初回起動時の `Wizard` コンポーネントおよび、OSのパッケージマネージャ (`winget`, `brew`)。

---

## 14. 変更・追加された仕様 (2026-05-21 設定画面からの環境診断再実行機能)

### フロントエンド (React): `SettingsModal.tsx`
- **概要**: ユーザーがいつでも初回起動時の「環境診断ウィザード」に戻って動作確認やシミュレーションを行えるように、設定画面に「環境診断の再実行」機能を追加しました。
- **追加関数**:
  - `handleRerunWizard() -> Promise<void>`
    - アプリ設定データ (`app_config.json`) の `setup_completed` を `false` に書き換えて保存し、`window.location.reload()` を実行してアプリケーションを再ロードします。
- **UI変更点**:
  - 設定モーダルの最下部に「6. 環境診断」セクションを追加。
  - アイコン `Cpu` を使用したプレミアムな「診断ウィザードを実行」ボタンを配置し、説明文を添えて配置しました。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `updateAppConfig` (API設定保存 / setup_completed の書き換え用)
  - `translation.json` (`settings.setup.title`, `settings.setup.desc`, `settings.setup.rerun_btn` の翻訳キー)
- **影響範囲 (Impact Scope)**:
  - `SettingsModal.tsx` (新規追加セクション)
  - `App.tsx` (リロード後、`setup_completed === false` になるため、初回起動時と同じウィザード画面が起動します)

---

## 15. 変更・追加された仕様 (2026-05-22 GitHub OAuth連携 ＆ クラウド同期 ＆ 新規リポジトリ作成実装)

### Rustバックエンド (Rust Commands)
- **関連ファイルパス**:
  - `app-file-timemachine/src-tauri/src/commands/github.rs` [NEW]
  - `app-file-timemachine/src-tauri/src/commands/git.rs` (末尾への追記)
  - `app-file-timemachine/src-tauri/src/commands/app_config.rs`
- **追加された Rust Tauri コマンド**:
  - `github_import_cli_token() -> Result<String, String>`
    - Windows/macOS等のPC内の GitHub CLI (`gh` コマンド) の認証状況をチェックし、`gh auth token` を用いて一瞬で認証トークンを自動抽出してフロントエンドに返すコマンド。
  - `git_get_remote(path: String) -> Result<Option<String>, String>`
    - 現在開いているプロジェクトの Git 設定ファイルから、すでに登録されてい## 17. 変更・追加された仕様 (2026-05-22 GitGraphマルチブランチ・ダークモード背景・管理モードUI・完全フォーマル多言語化)

### Rustバックエンド: `commands/git.rs` (`git_log`)
- **概要**: Gitのコミット履歴を取得する `git_log` コマンドに `--all` 引数を追加し、カレントブランチだけでなく、ローカルおよびリモートのすべてのブランチ履歴を可視化可能に拡張。
- **変更内容**: 
  - `git log` コマンドの構築部分に `.arg("--all")` を追記。
- **依存関係マッピング**:
  - **依存元**: フロントエンドの `GitGraph.tsx` から呼び出される `git_log` API。
  - **影響範囲**: `GitGraph` の路線図描画（Metroスタイル）。他ブランチの分岐・合流が路線図に自動プロットされるようになります。

### フロントエンド: `GitGraph.css` (ダークモード白浮きバグ修正)
- **概要**: ダークモード有効時に GitGraph のヘッダー背景が白く浮いてしまうデザイン上の不整合を修正。
- **変更内容**:
  - `.git-graph-header` の `background-color` を `var(--bg-surface, #ffffff)` から、他のパネルと調和する `var(--panel-bg, #f8fafc)` へ変更。
- **依存関係マッピング**:
  - **依存元**: CSS変数 `--panel-bg` (テーマ別配色システム)
  - **影響範囲**: GitGraph ヘッダーおよび検索バー周辺のビジュアル。

### フロントエンド: `FileTree` コンポーネント (管理モードバッジの省スペース化とレイアウト崩れ防止)
- **概要**: サイドバーを細く縮めた際に、ファイルツリー上部の管理モード切り替えバッジが改行されたり崩れたりするのを防ぐとともに、UI表示をシンプルかつ分かりやすく改善。
- **関連ファイル**: `FileTree.tsx` / `FileTree.css`
- **変更内容**:
  - バッジ内の「管理モード:」プレフィックスを削除し、簡潔なバッジ表記（「ホワイトリスト」/「ブラックリスト」）へ変更。
  - バッジのツールチップを、`git_mode` の状態に応じて `tooltip_whitelist` / `tooltip_blacklist` に動的に切り替えるように拡張。
  - CSSクラス `.git-mode-badge` に `white-space: nowrap` と `flex-shrink: 0` を追加し、改行や潰れによる崩れを完全にブロック。
- **依存関係マッピング**:
  - **依存元**: `react-i18next` (`t()`), 翻訳リソース内の `git_mode` 固有ツールチップキー
  - **影響範囲**: ファイルツリーのヘッダー領域、およびレスポンシブ配置。

### 翻訳リソース: `translation.json` (完全多言語化・フォーマル化・表記洗練)
- **概要**: プレースホルダーの多言語対応、日本語表示におけるカジュアル表現の完全フォーマル化（です・ます調統一）、および英語ライトテーマの不要文言排除と英語メッセージの標準化。
- **関連ファイル**: `locales/ja/translation.json`, `locales/en/translation.json`, `SyncSettingsModal.tsx`
- **変更内容**:
  - **プレースホルダーの多言語対応**: `SyncSettingsModal.tsx` のリポジトリ名入力枠の placeholder を `t("settings.sync.placeholder_repo_name")` に置き換え、日本語は `"例: my-awesome-project"`、英語は `"e.g., my-awesome-project"` と動的に切り替わるように対応。
  - **日本語のフォーマル統一**: クラウド同期設定モーダルおよびGitHub連携内のカジュアルな日本語語尾（「〜だよ！」「〜してね」等）をすべて「〜です・ます」「〜してください」のビジネス仕様に修正。
  - **英語ライトテーマ表記の洗練**: Theme設定 of `theme.light` から `(GitHub Recommended)` という不要な文言を排除し、シンプルな `"Light"` に修正。
  - **英語メッセージの文末記号標準化**: 英語翻訳内の感嘆符（`!`）をフォーマルなピリオド（`.`）に統一。
- **依存関係マッピング**:
  - **依存元**: `i18next`, `react-i18next` (`useTranslation`)
  - **影響範囲**: アプリ全体の設定画面、同期モーダル、インストールウィザード、および言語切り替え時の表示。

---

## 18. 変更・追加された仕様 (2026-05-22 本物の安全ガード (Safety Scan) の実装とOS固有ファイルの網羅的スキャン化)

### Rustバックエンド: `commands/git.rs` (`git_get_uncommitted_files`)
- **概要**: Gitリポジトリ内の未コミットの変更ファイル（新規追加・修正・リネームされたファイル）の一覧とそれぞれのファイルサイズを取得するコマンドを実装。
- **仕様**:
  - `git status --porcelain` コマンドを内部で実行し、出力を1行ずつパース。
  - 削除されたファイル（ステータス 'D'）はチェック対象外としてスキップ。
  - リネーム（ステータス 'R'）が発生したファイルについては、`old_path -> new_path` の形式から `new_path` 部分を正しくパースして抽出し、ファイルパスとサイズを検出。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: `std::process::Command` による外部Gitコマンド実行、`dunce::canonicalize` による安全なOSに依存しないパス処理。
  - **影響範囲 (Impact Scope)**: フロントエンド `MainLayout.tsx` 内の `handleSaveClick` スキャン。

### フロントエンド: `utils/safety.ts` (`analyzeFilesForSafety`)
- **概要**: 保存対象のファイルをスキャンし、セキュリティ上の懸念（秘密鍵、環境変数設定ファイルなど）や、各OS固有の不要なゴミファイル（macOS: `.DS_Store`、Windows: `Thumbs.db`等）、および巨大ファイル（100MB超）を堅牢に検知して警告する解析ロジックを実装。
- **仕様**:
  - **DANGEROUS_EXTENSIONS**: `.env`, `.pem`, `.key`, `.p12`, `.pfx`, `.der`, `.pub`, `.pkcs12`（セキュリティ） / Windows実行ファイル、macOSシステムファイル、一時ファイルなどの拡張子を定義。
  - **DANGEROUS_FILENAMES**: `thumbs.db`, `desktop.ini` (Windows), `.ds_store`, `.apple_double` (macOS), `.directory` (Linux), `id_rsa` (SSH秘密鍵) などのファイル名を大文字小文字に関わらず完全一致で検知。
  - **DANGEROUS_PREFIXES / DANGEROUS_SUFFIXES**: `._`（macOS一時ファイル）や `~`（エディタバックアップ）などの前方/後方一致検知を実装。
  - **DANGEROUS_PATTERNS**: `node_modules/`, `.git/`, `.env.local` などのパス部分一致パターンを定義。
  - **分類マッピング**: 
    - セキュリティリスクは `security_risk_extension`（秘密鍵や設定ファイル）に分類。
    - OS不要ファイル、システム実行可能ファイル、一時ファイル、危険パターンは `inappropriate_directory`（管理対象外にすべきファイル）に分類。
    - 100MB超は `large_file`（巨大ファイル）に分類。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: なし (Pure TypeScript ユーティリティ)
  - **影響範囲 (Impact Scope)**: `MainLayout.tsx` 内の `handleSaveClick` の自動スキャンロジック、および `SafetyDialog.tsx` の表示テキスト。

### フロントエンド: `MainLayout.tsx` (安全ガードの統合および自動除外処理)
- **概要**: 保存・コミット時に実際の未コミット変更ファイルを Rust バックエンド経由で取得し、上記の `analyzeFilesForSafety` を用いてスキャンを実行。検出された危険なファイルをダイアログからワンクリックで除外（`.gitignore` への追加）できるフローを実装。
- **仕様**:
  - **安全ガードの動的割込み**: 保存ボタン押下時、設定 `settings_auto_scan` が有効な場合は `git_get_uncommitted_files` で変更ファイルを取得。`analyzeFilesForSafety` で問題が検出された場合は `SafetyDialog` をポップアップして保存を一時停止。
  - **`handleConfirmExclude` (自動除外・gitignore追加)**: ダイアログの「除外して保存」をクリックすると、検出された各ファイル/フォルダのフルパスと現在の `git_mode`（ホワイトリスト/ブラックリスト）に応じた形式で `update_gitignore` コマンドを呼び出し、自動的に `.gitignore` へ登録してからコミットを継続。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: `git_get_uncommitted_files` (Rust), `analyzeFilesForSafety` (utils), `update_gitignore` (Rust), `proceedToSaveOrCommit` (フロントエンド)
  - **影響範囲 (Impact Scope)**: 状態保存（コミット）の全体フロー、`.gitignore` の永続化管理。
麗にカード内に表示。
    - **CLI自動インポートボタン**: PCにインストールされている GitHub CLI からワンクリックで安全にトークンを自動でインポートする。
    - **PAT手動入力**: Personal Access Token (PAT) を手動で入力して連携できるフォーム（入力チェック付き）。
    - **連携解除**: トークンとアカウント情報を完全に消去する。
  - **`SyncSettingsModal.tsx` (新規作成モーダル)**:
    - クラウド同期の際にリモート（origin）が設定されていない場合に自動でポップアップする設定モーダル。
    - **【超快適】新規リポジトリ直接作成機能**: アプリ内からリポジトリ名（必須）、説明、公開設定（Public/Private）、トピックス（カンマ区切り）を入力し、「作成して同期」を押すだけで、フロントから直接 GitHub API (`POST /user/repos`) にリクエストを送り、リポジトリを新規作成したのち、自動で `git_set_remote` し同期まで完了させる。
    - **既存リポジトリ紐付け機能**: 既存のGitリポジトリURLを origin リモートとしてワンクリックで紐付ける。
  - **`MainLayout.tsx` の同期ボタンと処理統合**:
    - フッターのコントロール領域に、現代のSaaSに最適な枠線デザインの「同期」ボタンを追加。
    - **同期処理の完全な自動フロー**:
      1. 同期ボタン押下時、プロジェクトフォルダ選択の有無と、GitHub連携状態をチェック。未連携なら設定画面をポップアップ。
      2. `git_get_remote` で origin リモートの有無を判定。未設定なら `SyncSettingsModal` をポップアップ。
      3. リモート設定完了後、`git_pull` を実行。マージ競合が発生した（Rustから `"CONFLICT"` が返された）場合は、すでに実装済みの `ConflictResolverModal` をポップアップさせて同期を一時停止し、ユーザーに手動で解決させる。
      4. Pull 成功後、自動的に `git_push` を行い、バックアップと他端末との同期を一度のクリックで完了させる。

## 11. 変更・追加された仕様 (2026-05-21 GitGraphツリー廃止と落ち着いたプレミアムフッターボタン)

### GitGraph (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx` / `GitGraph.css`
- **変更点**:
  - `graphStyle` 状態および「ツリー/路線図」表示切り替えトグルUIを完全に廃止し、美しく整理された「路線図（Metroスタイル）」に一本化しました。
  - `getTemplate` 関数から `style` 引数を排除し、常にCUD（岡部・柴田パレット）準拠のカラフルなMetroスタイルが適用されるように修正。
- **影響範囲**: `GitGraph` コンポーネントおよびUIの簡素化。不要になった `.graph-style-toggle` や `.toggle-btn` のCSSスタイルを `GitGraph.css` からクリーンアップ。

### フッターアクションボタンのプレミアム刷新
- **ファイルパス**: `app-file-timemachine/src/components/layout/MainLayout.tsx` / `MainLayout.css`
- **変更点**:
  - **マージボタンのアイコン変更**: マージボタン（`merge-to-main-btn`）のアイコンを `lucide-react` の `Check` から、よりコンテキストを体現する `GitMerge` アイコンに変更しました。
  - **プレミアムなマージボタンスタイリング**: クラス `.merge-to-main-btn` に新規スタイルを適用。ギラギラしたネオンや派手なエフェクトを完全に排除し、職場で使える品格のある「大人のプレミアムグリーン（マージ成功を象徴）」として定義。
  - **ホバー・インタラクションの最適化**: マウスホバー時に微細な上昇（`translateY(-1px)`）と落ち着いたグリーンのドロップシャドウを付与し、滑らかに遷移（`transition`）するインタラクションを実装。
  - **フッター内ボタンの一貫性 (高さを36pxに統一)**:
    - フッター内の全ボタン（`branch-info-btn`（現在のルート）, `create-branch-btn`（新しいルート）, `merge-to-main-btn`（マージ）, `save-state-btn`（状態を保存））の `height` を `36px`（`box-sizing: border-box`）に統一。
    - 各ボタンの余白、ボーダー、角丸（`border-radius: 6px`）を揃え、隣接ボタンと完全に整列するプロ仕様の美しいフッターUIにリニューアル。
    - **ライト・ダークモード双方の最適化**:
      - 各ボタンで `[data-theme="dark"]` によるホバー明度とシャドウ濃度の微調整を徹底し、白背景（ライトモード）でも黒背景（ダークモード）でもプレミアムで落ち着いた見栄えを保証。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `App.css` で定義された CSS変数（`--accent-color`, `--success-color`, `--hover-bg`, `--border-color` 等）
  - `lucide-react`（`GitMerge` アイコン）
- **影響範囲 (Impact Scope)**:
  - `MainLayout.tsx` のフッターレンダリング部分。
  - アプリフッターの全体のビジュアル・レイアウト。

---

## 12. 変更・追加された仕様 (2026-05-21 GitGraphブランチ路線と名前のカラー同期化)

### GitGraph (React Component)
- **ファイルパス**: `app-file-timemachine/src/components/graph/GitGraph.tsx` / `GitGraph.css`
- **追加された状態 (State)**:
  - `commitBranchMap: Map<string, { name: string; color: string }>`
    - 各コミットハッシュに対する「所属ブランチ名」および「割り当てられた路線色」の対応マッピングを記録するReact状態。
- **カラー追跡・同期ロジック**:
  - `gitgraph` のDOMレンダリングを制御する `useEffect` 内で、古い順にコミットをプロットする際、ブランチの新規生成タイミングをフックし、バリアフリー対応の CUD カラーパレット（`#56b4e9`（スカイブルー）、`#e69f00`（オレンジ）、`#009e73`（青緑）、`#cc79a7`（マゼンタピンク））から順に色を割り当てます。
  - **ブランチ数無限増加への安全設計**: カラー割り当て時に、ブランチ数が増加してもエラーにならないよう `cudColors[nextColorIndex++ % cudColors.length]` のように循環モジュロ演算（`%`）を適用。
  - ループ内でマージ、分岐、通常の直系コミットなど、すべてのコミットパターンで所属ブランチ名と色が正確に同期・蓄積されます。
  - 計算完了後、無限再レンダリングを防止するため、直前の `commitBranchMap` と差異が生じた場合のみ `setCommitBranchMap` を更新する保護ロジックを実装。
- **HTML UIの改善**:
  - **所属路線ドットインジケーターの追加**: コミット履歴（HTML）の各行（`.commit-info-wrapper`）の左端に、所属路線と同じ色を持つ直径8pxのドット（`.commit-branch-dot`）を追加。バッジがない中間コミットの所属ブランチも一目で判別可能に。
  - **ブランチ名バッジの動的カラー化**: HEAD位置のブランチ名バッジ（`.commit-branch-badge`）にインラインで `style` 属性を適用し、所属路線の色から透過背景色（`color + "20"` / 透過度約12%）、文字色（`color`）、透過ボーダー枠線（`color + "40"` / 透過度約25%）を動的に生成して適用。
  
#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `cudColors` (CUDバリアフリー配色パレット)
  - `commits` (履歴配列 / 描画ループと同期トリガーに使用)
- **影響範囲 (Impact Scope)**:
  - `GitGraph.tsx` の HTML 履歴リストのレンダリング部、および各バッジ、新規追加ドット（`.commit-branch-dot`）。
  - `GitGraph.css` のクラス構造。

### 翻訳リソース (`translation.json`) の微修正
- **ファイルパス**: `app-file-timemachine/public/locales/ja/translation.json`
- **変更点**:
  - 設定画面のテーマ変更オプション `settings.theme.light` の値を、ユーザーの要望に基づき「ライト (GitHubオススメ)」から「ライト」へシンプルに変更し、UI文言から余分な表記を排除。

### 設定保存ファイル (`app_config.json`) の並行書き込み競合バグの根本解決
- **関連ファイルパス**:
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx`
  - `app-file-timemachine/src/api/config.ts`
  - `app-file-timemachine/src-tauri/src/commands/app_config.rs`
- **追加された状態・変数**:
  - `SettingsModal.tsx` 内の `isLoaded: boolean` (設定ロード完了フラグ)
  - `config.ts` 内の `updateQueue: Promise<void>` (書き込み直列化キュー)
- **バグの根本原因と解決ロジック**:
  - **根本原因**: `SettingsModal` のマウント時、非同期ロード前にReactの初期値が設定保存 `useEffect` をトリガーし、設定がデフォルトで上書きされる問題がありました。さらに、複数の処理から `updateAppConfig` が同時に呼ばれると、「Read-Modify-Write」の過程で並行書き込み競合（Race Condition）が発生し、JSONファイル末尾に `}}` などの余分な文字が書き込まれてパースエラー（`trailing characters`）を引き起こし、起動時にアプリが白くなったりセットアップ無限ループに陥る致命的なバグになっていました。
  - **解決策1 (ガード付き一括保存)**: `SettingsModal.tsx` にて、非同期ロードが完了するまで保存処理をブロックする `isLoaded` ガードを追加し、保存処理を単一の `useEffect` に一括統合してReactマウント時の不要な上書きを防止しました。
  - **解決策2 (書き込み直列化キュー)**: `config.ts` の `updateAppConfig` に Promise キューを導入し、複数箇所から同時に設定更新要求が来ても必ず1つずつ順番に処理（直列化）されるようにし、ファイル書き込みの競合を完全にブロックしました。
  - **解決策3 (JSON破損時の自己修復機能)**: `app_config.rs` の設定読み込み時、もしJSONのパースに失敗した場合でもエラーでアプリを止めるのではなく、壊れたファイルを `.bak` に退避させてデフォルト設定を返す「自己修復機能」を実装しました。これにより、次回保存時にクリーンなJSONで上書きされる安全な設計になっています。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `updateQueue` (API設定保存の直列化管理)
  - `isLoaded` (SettingsModalのロード後ガード判定)
- **影響範囲 (Impact Scope)**:
  - アプリ全体の設定保存フロー (`updateAppConfig` 呼び出し箇所すべて)
  - Tauri側の設定ファイル読み込みフォールバック処理 (`get_app_config`)

---

---

## 13. 変更・追加された仕様 (2026-05-21 セットアップウィザード＆シミュレーションモード実装)

### Rustバックエンド: `commands/setup.rs`
- **概要**: 依存関係（Git, GitHub CLI, Homebrew等）のインストール状態を診断し、未インストールの場合はOSネイティブのパッケージマネージャを呼び出して自動インストールを実行するコマンド。UIデザインや挙動の検証ができるよう「シミュレーションモード（デバッグモード）」を備える。
- **追加/変更関数**:
  - `check_dependencies(simulate: Option<bool>) -> Result<DependencyStatus, String>`
    - 既存コマンドの引数に `simulate` を追加。`true` の場合、実際はインストールされていてもすべて `false` (未インストール) を返却し、未インストール状態をシミュレーションします。
  - `install_dependency(app: tauri::AppHandle, tool: String, simulate: bool) -> Result<(), String>`
    - 新規追加コマンド。引数 `tool` (`"git" | "gh"`) に応じて自動インストールを実行します。
    - **Windows**: `winget install --silent ...` を子プロセスで実行。
    - **macOS**: `xcode-select --install` または `brew install gh` を子プロセスで実行。
    - **シミュレーション**: 実際にはコマンドを実行せず、タイマー (`tokio::time::sleep`) を使って数秒間かけて架空の進捗ログを出力します。
    - **リアルタイムログ**: 子プロセスの `stdout` / `stderr` を `BufReader` で行ごとに読み取り、`tauri::AppHandle::emit` を使用してフロントエンドへ `install-log` イベントとしてリアルタイムにストリーミング配信します。

### フロントエンド (React): `Wizard.tsx`
- **概要**: アプリケーション初回起動時の環境診断と自動セットアップを行うウィザードUIコンポーネント。
- **追加状態とロジック**:
  - `isSimulated` (状態): 右上のトグルスイッチで切り替えられるデバッグモードのフラグ。切り替え時に再度 `check_dependencies` が呼び出されます。
  - `installingTool`, `installProgress`, `installLogs` (状態): インストールの進行状況とログメッセージを管理。
  - `handleInstall` (関数): `install_dependency` を呼び出すと同時に、Tauriの `listen("install-log")` を使用してバックエンドからのリアルタイムログを受信し、状態を更新します。
  - **Git必須化のロジック**: アプリケーションの進行条件を `allInstalled = status.git` に変更し、GitHub CLI を「(任意) / (Optional)」扱いとしました。Git さえインストールされていれば「完了してアプリを起動」ボタンが有効になります。
- **UI更新**:
  - **シミュレーショントグル**: 画面右上にデバッグ用のトグルボタンを配置。
  - **プログレスバー＆ターミナル風UI**: インストール実行中、CSSアニメーション付きのプログレスバーと、黒背景のターミナル風領域（`.wizard-terminal`）にリアルタイムで流れるログが表示されます。ログが追加されるたびに一番下へ自動スクロール (`logsEndRef.scrollIntoView`) します。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `tauri::AppHandle::emit` (Rust -> React イベント通知)
  - `@tauri-apps/api/event` (React側のイベントリスナー `listen`)
  - `translation.json` (追加された `setup.optional`, `setup.install_btn`, `setup.installing_status`, `setup.simulation_mode` 等のキー)
  - `tokio` (Rustの非同期ランタイム / `time::sleep` によるシミュレーション遅延用)
- **影響範囲 (Impact Scope)**:
  - 初回起動時の `Wizard` コンポーネントおよび、OSのパッケージマネージャ (`winget`, `brew`)。

---

## 14. 変更・追加された仕様 (2026-05-21 設定画面からの環境診断再実行機能)

### フロントエンド (React): `SettingsModal.tsx`
- **概要**: ユーザーがいつでも初回起動時の「環境診断ウィザード」に戻って動作確認やシミュレーションを行えるように、設定画面に「環境診断の再実行」機能を追加しました。
- **追加関数**:
  - `handleRerunWizard() -> Promise<void>`
    - アプリ設定データ (`app_config.json`) の `setup_completed` を `false` に書き換えて保存し、`window.location.reload()` を実行してアプリケーションを再ロードします。
- **UI変更点**:
  - 設定モーダルの最下部に「6. 環境診断」セクションを追加。
  - アイコン `Cpu` を使用したプレミアムな「診断ウィザードを実行」ボタンを配置し、説明文を添えて配置しました。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `updateAppConfig` (API設定保存 / setup_completed の書き換え用)
  - `translation.json` (`settings.setup.title`, `settings.setup.desc`, `settings.setup.rerun_btn` の翻訳キー)
- **影響範囲 (Impact Scope)**:
  - `SettingsModal.tsx` (新規追加セクション)
  - `App.tsx` (リロード後、`setup_completed === false` になるため、初回起動時と同じウィザード画面が起動します)

---

## 15. 変更・追加された仕様 (2026-05-22 GitHub OAuth連携 ＆ クラウド同期 ＆ 新規リポジトリ作成実装)

### Rustバックエンド (Rust Commands)
- **関連ファイルパス**:
  - `app-file-timemachine/src-tauri/src/commands/github.rs` [NEW]
  - `app-file-timemachine/src-tauri/src/commands/git.rs` (末尾への追記)
  - `app-file-timemachine/src-tauri/src/commands/app_config.rs`
- **追加された Rust Tauri コマンド**:
  - `github_import_cli_token() -> Result<String, String>`
    - Windows/macOS等のPC内の GitHub CLI (`gh` コマンド) の認証状況をチェックし、`gh auth token` を用いて一瞬で認証トークンを自動抽出してフロントエンドに返すコマンド。
  - `git_get_remote(path: String) -> Result<Option<String>, String>`
    - 現在開いているプロジェクトの Git 設定ファイルから、すでに登録されている `origin` リモートURLを取得して返すコマンド。
  - `git_set_remote(path: String, remote_url: String) -> Result<(), String>`
    - `origin` リモートを設定または上書き（既存リモートのクリアを含む）するコマンド。
  - `git_push(path: String, token: String, branch: String) -> Result<String, String>`
    - トークンが Git 設定ファイルに平文で保存されてしまう漏洩リスクを防ぐため、Git設定には書き込まず、コマンド引数に一時的な `https://<token>@github.com/...` 形式の認証付きURLを動的に組み立てて安全に `git push -u` を実行するコマンド。
  - `git_pull(path: String, token: String, branch: String) -> Result<String, String>`
    - 安全な一時的認証URLを用いて `git pull` を実行するコマンド。標準出力・エラー出力の中に `CONFLICT` という文字列を検知した場合は、専用エラーとして `"CONFLICT"` 文字列を返し、フロントの競合解決モーダルへと接続します。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - Windows CLI 子プロセス (`cmd /C gh auth token` または `gh auth token` 実行環境)
  - Git ローカル環境の `git remote`, `git push`, `git pull` コマンド
- **影響範囲 (Impact Scope)**:
  - `lib.rs` へのハンドラ登録。
  - フロントエンドの設定画面、および同期アクションボタン。

### フロントエンド (React / TypeScript UI)
- **関連ファイルパス**:
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx` / `SettingsModal.css` (プレミアム拡張)
  - `app-file-timemachine/src/components/settings/SyncSettingsModal.tsx` [NEW] / `SyncSettingsModal.css` [NEW]
  - `app-file-timemachine/src/components/layout/MainLayout.tsx` / `MainLayout.css`
- **追加されたUI ＆ 機能ロジック**:
  - **`SettingsModal.tsx` のプレミアム拡張**:
    - **アカウント情報カード**: 設定されたトークンを基に GitHub API (`/user`) をバックグラウンドで叩き、ユーザーのアイコン画像（アバター）、ユーザー名、ログイン名を綺麗にカード内に表示。
    - **CLI自動インポートボタン**: PCにインストールされている GitHub CLI からワンクリックで安全にトークンを自動でインポートする。
    - **PAT手動入力**: Personal Access Token (PAT) を手動で入力して連携できるフォーム（入力チェック付き）。
    - **連携解除**: トークンとアカウント情報を完全に消去する。
  - **`SyncSettingsModal.tsx` (新規作成モーダル)**:
    - クラウド同期の際にリモート（origin）が設定されていない場合に自動でポップアップする設定モーダル。
    - **【超快適】新規リポジトリ直接作成機能**: アプリ内からリポジトリ名（必須）、説明、公開設定（Public/Private）、トピックス（カンマ区切り）を入力し、「作成して同期」を押すだけで、フロントから直接 GitHub API (`POST /user/repos`) にリクエストを送り、リポジトリを新規作成したのち、自動で `git_set_remote` し同期まで完了させる。
    - **既存リポジトリ紐付け機能**: 既存のGitリポジトリURLを origin リモートとしてワンクリックで紐付ける。
  - **`MainLayout.tsx` の同期ボタンと処理統合**:
    - フッターのコントロール領域に、現代のSaaSに最適な枠線デザインの「同期」ボタンを追加。
    - **同期処理の完全な自動フロー**:
      1. 同期ボタン押下時、プロジェクトフォルダ選択の有無と、GitHub連携状態をチェック。未連携なら設定画面をポップアップ。
      2. `git_get_remote` で origin リモートの有無を判定。未設定なら `SyncSettingsModal` をポップアップ。
      3. リモート設定完了後、`git_pull` を実行。マージ競合が発生した（Rustから `"CONFLICT"` が返された）場合は、すでに実装済みの `ConflictResolverModal` をポップアップさせて同期を一時停止し、ユーザーに手動で解決させる。
      4. Pull 成功後、自動的に `git_push` を行い、バックアップと他端末との同期を一度のクリックで完了させる。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `github_import_cli_token`, `git_get_remote`, `git_set_remote`, `git_push`, `git_pull` (Rust コマンド)
  - GitHub API (`https://api.github.com/user`, `/user/repos`, `/repos/{owner}/{repo}/topics`)
  - `updateAppConfig`, `getAppConfig` (API/設定の保存キュー)
- **影響範囲 (Impact Scope)**:
  - `MainLayout.tsx` のフッターコントロールおよび非同期イベントループ。
  - 設定モーダルのUIデザイン。

---

## 16. 変更・追加された仕様 (2026-05-22 GitHub連携UIローカライズおよびレイアウトの縦並び調整)

### フロントエンド (React / TypeScript / CSS)
- **関連ファイルパス**:
  - `app-file-timemachine/src/components/settings/SyncSettingsModal.tsx` (多言語化対応)
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx` (ブランドアイコン自前化 ＆ 直書き文字列排除)
  - `app-file-timemachine/src/components/settings/SettingsModal.css` (ボタン縦並び化)
  - `app-file-timemachine/public/locales/ja/translation.json` (クラウド同期用翻訳の追加)
  - `app-file-timemachine/public/locales/en/translation.json` (クラウド同期用翻訳の追加)
- **変更内容**:
  - **`SyncSettingsModal.tsx` の多言語化 (i18n)**:
    - 以前はハードコードされていた「クラウド同期の設定」「GitHubに新しく作る」「既存リポジトリを紐付ける」等の日本語文字列を、すべて `translation.json` (ja, en) に新設した `settings.sync` 以下のキーを参照する `t()` 関数コールに変更しました。
  - **連携用ボタンの縦並び（上下）配置への変更 (`SettingsModal.css`)**:
    - 設定画面内の「GitHub CLIから自動インポート」と「PAT(トークン)を手動入力」のボタンについて、横並びから上下の縦並びへと変更しました。
    - クラス `.github-action-buttons` に対し `flex-direction: column` と `width: 100%` を適用し、ボタン内の `flex: 1.2` 等の不要な横伸縮定義を排除。これにより、縦並びになった際も親要素に美しくフィットし、余白バランスが取れたモダンなUIへと洗練されました。
  - **ブランドアイコン (GitHub) の自前定義による安全化 (`SettingsModal.tsx`)**:
    - 本プロジェクトの `lucide-react` バージョンでブランドアイコン `Github` がエクスポートされていない問題を解決するため、`SettingsModal.tsx` 内にインラインで著作権的に安全な GitHub (オクトキャット) の SVG コンポーネント `Github` を自前定義しました。これにより、外部コードの書き換えなしで安全・確実に美しいアイコンを表示できるようになりました。
  - **`SettingsModal.tsx` の残存ハードコード修正**:
    - アカウントカード内で直書きされていた `"連携済み"` を `t("settings.github.connected")` に完全に置き換え、多言語化漏れを完全になくしました。

#### 依存関係マッピング (Dependency Tracking)
- **依存元 (Dependencies)**:
  - `translation.json` (新設した `settings.sync` セクション)
  - 自前定義の `Github` SVG コンポーネント
- **影響範囲 (Impact Scope)**:
  - 設定画面 (`SettingsModal`) およびクラウド同期モーダル (`SyncSettingsModal`) のレンダリングとローカライズ処理。
  - ビルド成果物のコンパイル安定性 (TypeScriptエラーの完全解消)。

---

## 16. 変更・追加された仕様 (2026-05-22 GitGraphマルチブランチ・ダークモード背景・管理モードUI・完全フォーマル多言語化)

### Rustバックエンド: `commands/git.rs` (`git_log`)
- **概要**: Gitのコミット履歴を取得する `git_log` コマンドに `--all` 引数を追加し、カレントブランチだけでなく、ローカルおよびリモートのすべてのブランチ履歴を可視化可能に拡張。
- **変更内容**: 
  - `git log` コマンドの構築部分に `.arg("--all")` を追記。
- **依存関係マッピング**:
  - **依存元**: フロントエンドの `GitGraph.tsx` から呼び出される `git_log` API。
  - **影響範囲**: `GitGraph` の路線図描画（Metroスタイル）。他ブランチの分岐・合流が路線図に自動プロットされるようになります。

### フロントエンド: `GitGraph.css` (ダークモード白浮きバグ修正)
- **概要**: ダークモード有効時に GitGraph のヘッダー背景が白く浮いてしまうデザイン上の不整合を修正。
- **変更内容**:
  - `.git-graph-header` の `background-color` を `var(--bg-surface, #ffffff)` から、他のパネルと調和する `var(--panel-bg, #f8fafc)` へ変更。
- **依存関係マッピング**:
  - **依存元**: CSS変数 `--panel-bg` (テーマ別配色システム)
  - **影響範囲**: GitGraph ヘッダーおよび検索バー周辺のビジュアル。

### フロントエンド: `FileTree` コンポーネント (管理モードバッジの省スペース化とレイアウト崩れ防止)
- **概要**: サイドバーを細く縮めた際に、ファイルツリー上部の管理モード切り替えバッジが改行されたり崩れたりするのを防ぐとともに、UI表示をシンプルかつ分かりやすく改善。
- **関連ファイル**: `FileTree.tsx` / `FileTree.css`
- **変更内容**:
  - バッジ内の「管理モード:」プレフィックスを削除し、簡潔なバッジ表記（「ホワイトリスト」/「ブラックリスト」）へ変更。
  - バッジのツールチップを、`git_mode` の状態に応じて `tooltip_whitelist` / `tooltip_blacklist` に動的に切り替えるように拡張。
  - CSSクラス `.git-mode-badge` に `white-space: nowrap` と `flex-shrink: 0` を追加し、改行や潰れによる崩れを完全にブロック。
- **依存関係マッピング**:
  - **依存元**: `react-i18next` (`t()`), 翻訳リソース内の `git_mode` 固有ツールチップキー
  - **影響範囲**: ファイルツリーのヘッダー領域、およびレスポンシブ配置。

### 翻訳リソース: `translation.json` (完全多言語化・フォーマル化・表記洗練)
- **概要**: プレースホルダーの多言語対応、日本語表示におけるカジュアル表現の完全フォーマル化（です・ます調統一）、および英語ライトテーマの不要文言排除と英語メッセージの標準化。
- **関連ファイル**: `locales/ja/translation.json`, `locales/en/translation.json`, `SyncSettingsModal.tsx`
- **変更内容**:
  - **プレースホルダーの多言語対応**: `SyncSettingsModal.tsx` のリポジトリ名入力枠の placeholder を `t("settings.sync.placeholder_repo_name")` に置き換え、日本語は `"例: my-awesome-project"`、英語は `"e.g., my-awesome-project"` と動的に切り替わるように対応。
  - **日本語のフォーマル統一**: クラウド同期設定モーダルおよびGitHub連携内のカジュアルな日本語語尾（「〜だよ！」「〜してね」等）をすべて「〜です・ます」「〜してください」のビジネス仕様に修正。
  - **英語ライトテーマ表記の洗練**: Theme設定 of `theme.light` から `(GitHub Recommended)` という不要な文言を排除し、シンプルな `"Light"` に修正。
  - **英語メッセージの文末記号標準化**: 英語翻訳内の感嘆符（`!`）をフォーマルなピリオド（`.`）に統一。
- **依存関係マッピング**:
  - **依存元**: `i18next`, `react-i18next` (`useTranslation`)
  - **影響範囲**: アプリ全体の設定画面、同期モーダル、インストールウィザード、および言語切り替え時の表示。

---

## 17. 変更・追加された仕様 (2026-05-22 本物の安全ガード (Safety Scan) の実装とOS固有ファイルの網羅的スキャン化)

### Rustバックエンド: `commands/git.rs` (`git_get_uncommitted_files`)
- **概要**: Gitリポジトリ内の未コミットの変更ファイル（新規追加・修正・リネームされたファイル）の一覧とそれぞれのファイルサイズを取得するコマンドを実装。
- **仕様**:
  - `git status --porcelain` コマンドを内部で実行し、出力を1行ずつパース。
  - 削除されたファイル（ステータス 'D'）はチェック対象外としてスキップ。
  - リネーム（ステータス 'R'）が発生したファイルについては、`old_path -> new_path` の形式から `new_path` 部分を正しくパースして抽出し、ファイルパスとサイズを検出。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: `std::process::Command` による外部Gitコマンド実行、`dunce::canonicalize` による安全なOSに依存しないパス処理。
  - **影響範囲 (Impact Scope)**: フロントエンド `MainLayout.tsx` 内の `handleSaveClick` スキャン。

### フロントエンド: `utils/safety.ts` (`analyzeFilesForSafety`)
- **概要**: 保存対象のファイルをスキャンし、セキュリティ上の懸念（秘密鍵、環境変数設定ファイルなど）や、各OS固有の不要なゴミファイル（macOS: `.DS_Store`、Windows: `Thumbs.db`等）、および巨大ファイル（100MB超）を堅牢に検知して警告する解析ロジックを実装。
- **仕様**:
  - **DANGEROUS_EXTENSIONS**: `.env`, `.pem`, `.key`, `.p12`, `.pfx`, `.der`, `.pub`, `.pkcs12`（セキュリティ） / Windows実行ファイル、macOSシステムファイル、一時ファイルなどの拡張子を定義。
  - **DANGEROUS_FILENAMES**: `thumbs.db`, `desktop.ini` (Windows), `.ds_store`, `.apple_double` (macOS), `.directory` (Linux), `id_rsa` (SSH秘密鍵) などのファイル名を大文字小文字に関わらず完全一致で検知。
  - **DANGEROUS_PREFIXES / DANGEROUS_SUFFIXES**: `._`（macOS一時ファイル）や `~`（エディタバックアップ）などの前方/後方一致検知を実装。
  - **DANGEROUS_PATTERNS**: `node_modules/`, `.git/`, `.env.local` などのパス部分一致パターンを定義。
  - **分類マッピング**: 
    - セキュリティリスクは `security_risk_extension`（秘密鍵や設定ファイル）に分類。
    - OS不要ファイル、システム実行可能ファイル、一時ファイル、危険パターンは `inappropriate_directory`（管理対象外にすべきファイル）に分類。
    - 100MB超は `large_file`（巨大ファイル）に分類。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: なし (Pure TypeScript ユーティリティ)
  - **影響範囲 (Impact Scope)**: `MainLayout.tsx` 内の `handleSaveClick` の自動スキャンロジック、および `SafetyDialog.tsx` の表示テキスト。

### フロントエンド: `MainLayout.tsx` (安全ガードの統合および自動除外処理)
- **概要**: 保存・コミット時に実際の未コミット変更ファイルを Rust バックエンド経由で取得し、上記の `analyzeFilesForSafety` を用いてスキャンを実行。検出された危険なファイルをダイアログからワンクリックで除外（`.gitignore` への追加）できるフローを実装。
- **仕様**:
  - **安全ガードの動的割込み**: 保存ボタン押下時、設定 `settings_auto_scan` が有効な場合は `git_get_uncommitted_files` で変更ファイルを取得。`analyzeFilesForSafety` で問題が検出された場合は `SafetyDialog` をポップアップして保存を一時停止。
  - **`handleConfirmExclude` (自動除外・gitignore追加)**: ダイアログの「除外して保存」をクリックすると、検出された各ファイル/フォルダのフルパスと現在の `git_mode`（ホワイトリスト/ブラックリスト）に応じた形式で `update_gitignore` コマンドを呼び出し、自動的に `.gitignore` へ登録してからコミットを継続。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: `git_get_uncommitted_files` (Rust), `analyzeFilesForSafety` (utils), `update_gitignore` (Rust), `proceedToSaveOrCommit` (フロントエンド)
  - **影響範囲 (Impact Scope)**: 状態保存（コミット）の全体フロー、`.gitignore` の永続化管理。

---

## 18. 変更・追加された仕様 (2026-05-22 完全多言語化の整備・ツールチップ表示位置改善・ヘルプ早見表のテーマ対応)

### 翻訳ファイル: `locales/*/translation.json` (完全多言語化の一括整備)
- **概要**: 英語 (`en`) と日本語 (`ja`) に追加された最新の翻訳キー（`safety` セクション、`settings.sync` クラウド同期項目、`help` 表、`tooltip` 類）を、他の全14言語の翻訳ファイルへ自動マージし、キー露出バグを完全防止。
- **変更内容**: 
  - `merge_i18n.js` スクリプトの実行により、キー構造の全言語統一と英語値によるフォールバック補填を完了。
- **依存関係マッピング**:
  - **依存元**: なし
  - **影響範囲**: 言語切り替え時の全言語におけるテキスト表示の整合性。

### フロントエンド: `Tooltip` コポーネント (`Tooltip.tsx` / `Tooltip.css`)
- **概要**: ツールチップの表示方向制御、折り返し対応、z-index 強化によるパネル内表示のバグ解消。
- **変更内容**:
  - `position` Props (`"top" | "bottom" | "left" | "right"`) を追加し、動的に表示方向クラスを割り当て。
  - `Tooltip.css` にて `white-space: normal`、`max-width: 250px`、`width: max-content` を定義し、長い説明書きが不自然に横長に突き抜ける問題を解決。
  - `.tooltip-content` の `z-index` を `10000` に引き上げ、パネルのリサイズハンドルやコンテンツ領域の下に潜り込まないように保証。
  - 各方向に対応したポジショニングと吹き出し矢印（arrow）のスタイル定義を完備。
- **依存関係マッピング**:
  - **依存元**: React Props
  - **影響範囲**: `MainLayout.tsx` 内の各ツールチップ呼び出し部（ヘッダー部では `position="bottom"` に指定し、`overflow: hidden` による上部クリッピングを完全に回避）。

### フロントエンド: 「ヘルプ ＆ Gitコマンド早見表」 (`HelpModal.tsx` / `HelpModal.css`)
- **概要**: モーダル色の CSS変数化によるライト・ダークモードの完全対応、およびガラス調背景やテーブル・コード表示の高級感向上。
- **変更内容**:
  - `HelpModal.css` のハードコードされた黒背景などを、プロジェクト定義済の CSS変数（`--bg-color`, `--panel-bg`, `--border-color`, `--text-color` など）へ移行。
  - モーダルオーバーレイに `backdrop-filter: blur(8px)` を追加し、現代的でプレミアムなガラスモルフィズム背景を実装。
  - テーブルの行ホバー時の極小ハイライト（`var(--hover-bg)`）の追加。
  - コマンドスニペット (`code` タグ) のフォントをモノスペース化し、エディタのような高級感のある背景色と枠線にリニューアル。
- **依存関係マッピング**:
  - **依存元**: CSS変数 (App.css), `lucide-react`
  - **影響範囲**: ヘルプ画面モーダルのライト/ダークモードでの表示およびビジュアル全体の高級感向上。

---

## 19. 変更・追加された仕様 (2026-05-22 全11言語完全翻訳マージ実行 ＆ 早見表アイコン配色バグ修正 ＆ ライトモードツールチップ配色微調整)

### 翻訳リソース: `locales/*/translation.json` (11言語完全多言語化の完結)
- **概要**: 英語・日本語コピーや省略で未完となっていた11言語（`ar`, `de`, `es`, `fr`, `hi`, `id`, `it`, `ko`, `pt`, `th`, `vi`）の `translation.json` に、プロ仕様の自然な現地語翻訳データを完璧に流し込み、完全多言語化を完結させました。
- **変更内容**: 
  - 自動マージスクリプト `apply_translations.js` を実行し、既存のキー構造（`help`, `tooltip`, `conflict`, `branch`, `graph`, `history`, `layout`, `tree` など）を維持したまま、高品質な各国語データでマージ更新しました。
- **依存関係マッピング**:
  - **依存元**: なし (純粋な静的JSONリソース)
  - **影響範囲**: アプリケーション内のすべての多言語表示箇所（言語切替時に完全にローカライズされたテキストが提供されることを保証）。

### フロントエンド: 「ヘルプ ＆ Gitコマンド早見表」 (`HelpModal.tsx` / `HelpModal.css`)
- **概要**: 早見表モーダル内のアイコン色がテーマに関わらず Vite 既定の `#646cff` に固定されていたバグを修正し、テーマのアクセントカラーに連動させました。
- **変更内容**:
  - `HelpModal.tsx` 内で定義されていなかった `var(--primary-color)` のインラインCSS指定を削除し、クラス名 `.git-command-icon` を付与。
  - `HelpModal.css` に `.git-command-icon` クラスを定義し、カラーを `var(--accent-color, #3b82f6)` にマッピング。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: CSS変数 `--accent-color` (App.cssで管理)
  - **影響範囲 (Impact Scope)**: `HelpModal` 内のコマンド対比表のアイコン部分。テーマ連動が完璧に保証され、ライト/ダークモード双方で美しい調和を実現。

### フロントエンド: ツールチップ配色調整 (`App.css`)
- **概要**: ライトモード時におけるツールチップ枠線の境界線が浮き上がらないよう、デザインSkillsの「GitHubオススメ」に完全に調和させました。
- **変更内容**:
  - `App.css` 内の `:root`（ライトテーマ）における `--tooltip-border` 変数の値を、繊細な `#e2e8f0` に微調整。影表現（`--tooltip-shadow`）もソフトに調整。
- **依存関係マッピング**:
  - **依存元 (Dependencies)**: なし
  - **影響範囲 (Impact Scope)**: `Tooltip.css` を経由してアプリ全体のツールチップ枠線。

---

## 20. 変更・追加された仕様 (2026-05-22 GitHub連携機能の14言語完全ローカライズ)

### 翻訳リソース: `locales/*/translation.json` (GitHub連携の多言語化バグ修正)
- **概要**: 設定画面 (`SettingsModal.tsx`) で使用されている GitHub 連携関連のテキスト（`settings.github` セクション配下）が、日本語・英語以外の14言語でローカライズされず、英語のデフォルト値のまま放置されていたバグを解消しました。
- **変更内容**: 
  - 14言語（`ar`, `de`, `es`, `fr`, `hi`, `id`, `it`, `ko`, `pt`, `ru`, `th`, `vi`, `zh-CN`, `zh-TW`）の `translation.json` 内における `settings.github` 配下の10個の翻訳キー（`connected`, `disconnect`, `cli_import`, `pat_toggle`, `pat_placeholder`, `connect_btn`, `verifying`, `input_token_error`, `invalid_token_error`, `connection_error`）を、手抜きのない高品質なプロ仕様の現地語翻訳へ完全に更新・マージしました。
  - マージには Node.js スクリプト `apply_github_translations.js` を使用し、既存の JSON 構造や他の翻訳データを一切壊すことなく安全にアップデートしました。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: なし (純粋な静的JSONリソース)
  - **影響範囲 (Impact Scope)**: 設定画面のGitHub連携エリア (`SettingsModal.tsx`)。言語切替時に完全にローカライズされたテキストが提供されることを保証。

---

## 21. 変更・追加された仕様 (2026-05-22 ファイルプレビューヘッダーのFlexboxレイアウトバグ修正)

### フロントエンド: ファイルプレビューヘッダー (`FilePreview.tsx`)
- **概要**: ファイル名や絶対パスが非常に長い場合、あるいは画面幅が狭くなった場合に、Diffボタン（「差分(Diff)を表示」）が右端から画面外へ押し出されたり、押し潰されて消えてしまう表示崩れバグを完全に修正しました。
- **変更内容**:
  - 左側のコンテナ `div` に対し、`minWidth: 0` および `flex: 1` を指定して、Flexbox内での動的な縮小と `ellipsis` （三点リーダー）の動作を有効化。
  - `FileText` アイコンに `flexShrink: 0` を付与し、絶対パスが非常に長い時でもアイコンが潰れるのを防止。
  - `h3` （ファイル名表示部）に `overflow: 'hidden'`, `textOverflow: 'ellipsis'`, `whiteSpace: 'nowrap'` を明示的に指定し、確実に `...` 表示による余剰幅カットを保証。
  - 右側の `button` （Diffボタン）に `flexShrink: 0` を指定し、どれだけファイル名が長くても絶対に潰れることなく常に美しく右端に固定表示されるよう、極めて頑健なレイアウトにリニューアルしました。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `metadata` (ファイルメタデータ), `filePath` (フォールバックパス)
  - **影響範囲 (Impact Scope)**: ファイルプレビューコンポーネント (`FilePreview.tsx`) の上部ヘッダー部分。絶対パス表示時やウィンドウ幅縮小時でも、Diffボタンへのアクセス性を100%保証。

---

## 22. 変更・追加された仕様 (2026-05-22 非テキストファイル選択時のDiffボタンの非活性表示およびツールチップによる改善)

### フロントエンド: ファイルプレビューヘッダーのDiffボタン (`FilePreview.tsx`)
- **概要**: テキストファイル以外のバイナリファイル等（画像、音声、動画、または不明な形式）が選択された際、Diffボタンが画面から完全に消えてしまうことによるユーザーの混乱やバグとの誤認を防止するため、Diffボタンを常に表示し続けるように改善しました。
- **変更内容**:
  - `metadata?.file_type === "text"` という条件判定によるボタンの出し分けを廃止し、Diffボタンを常にDOM上にレンダリングします。
  - テキストファイル以外の場合は、ボタンに `disabled={metadata?.file_type !== "text"}` を付与してクリック操作を無効化します。
  - スタイルを動的に調整し、非テキストファイルの時は `color: "var(--text-muted)"`、`opacity: 0.5`、`cursor: "not-allowed"` を設定して、視覚的に非活性（disabled）状態であることが一目で伝わるように最適化しました。
  - `title` 属性にローカライズされたツールチップ `preview.action.diff_disabled_tooltip` を設定し、ホバー時に非活性の理由（「テキストファイルのみ差分を表示できます」など）がツールチップとして表示されるように対応しました。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `metadata` (ファイルメタデータ), `react-i18next` (`t()`), 翻訳リソース `translation.json` の `preview.action.diff_disabled_tooltip`
  - **影響範囲 (Impact Scope)**: ファイルプレビューコンポーネント (`FilePreview.tsx`) の上部ヘッダー部分。ユーザーに対し、テキストファイル以外で差分表示ができない仕様を直感的に伝え、混乱を完全に解消。

### 翻訳ファイル: `locales/*/translation.json` (非テキストファイル用ツールチップの16言語マージ)
- **概要**: ツールチップメッセージ `preview.action.diff_disabled_tooltip` を、すべてのサポート言語（16言語）で本物の高品質な現地語ローカライズを追加完了しました。
- **対応言語と翻訳内容**:
  - 日本語 (`ja`): `テキストファイルのみ差分を表示できます`
  - 英語 (`en`): `Diff can only be displayed for text files`
  - その他14言語 (`ar`, `de`, `es`, `fr`, `hi`, `id`, `it`, `ko`, `pt`, `ru`, `th`, `vi`, `zh-CN`, `zh-TW`) もそれぞれの言語に適合する自然なプロ仕様翻訳を適用しました。

---

## 23. 変更・追加された仕様 (2026-05-22 脆弱性対策とコード品質改善)

### Rustバックエンド: `commands/git.rs` (引数インジェクション対策のバリデーション実装)
- **概要**: ユーザー入力（ブランチ名、コミットハッシュなど）を介した Git コマンド引数インジェクション（Argument Injection）の脆弱性を防ぐための検証処理を実装。
- **追加された Rust 内部関数**:
  - `is_safe_git_ref(name: &str) -> bool`
    - 入力文字列が空でなく、ハイフン `-` から始まっておらず、制御文字、スペース、クォート（`'`, `"`）、バックスラッシュ（`\`）などの危険な文字を含んでいないかを検証するヘルパー関数。
- **変更された Tauri コマンド**:
  - `git_create_branch`, `git_checkout`, `git_delete_branch`, `git_merge_to_main`, `git_merge_abort`, `git_show_file_content`, `git_diff_file_commit`, `git_push`, `git_pull`
    - 各関数の先頭に入力引数の `is_safe_git_ref` 検証を追加。安全でない参照名が指定された場合は、外部コマンドを実行せずに即座に `Err` を返却するよう防護壁を構築。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `is_safe_git_ref` (内部バリデーション)
  - **影響範囲 (Impact Scope)**: フロントエンドから呼び出されるすべてのブランチ・コミット操作。ユーザーへのエラー伝播は従来のエラーハンドリングを流用するため、安全に安全ガード機能が有効になります。

### Rustバックエンド: 各種 clippy 警告の撲滅による品質向上
- **概要**: `cargo clippy` が出力したコード品質・安全性に関する警告（不要な `return`、不適切な参照渡し、無限ループの恐れ）を全面的に修正。
- **変更内容**:
  - `commands/setup.rs`:
    - 不要な `return` の削除（clippy `needless_return` 対策）。
    - 不要な借用の削除（clippy `needless_borrows_for_generic_args` 対策）。
    - `reader.lines().filter_map(|l| l.ok())` を `reader.lines().map_while(Result::ok)` に変更し、ストリームエラー時の無限ループリスクを完全に排除（clippy `lines_filter_map_ok` 対策）。Windows/macOS 双方のコードに適用。
  - `commands/files.rs`:
    - `trim_start_matches` でのクロージャを文字配列に変更（clippy `manual_pattern_char_comparison` 対策）。
  - `commands/preview.rs`:
    - `chardetng` の入力引数に対する不要な多重参照を解消（clippy `needless_borrow` 対策）。

---

## 24. 変更・追加された仕様 (2026-05-22 リアルタイムファイル監視および自動同期機能の追加)

### Rustバックエンド: ファイルシステム監視コマンドと状態管理 (`commands/watcher.rs` [NEW])
- **概要**: `notify` クレートを使用し、指定された管理対象フォルダ内のファイル変更（追加、編集、削除）をリアルタイムに検知する監視システムを実装。
- **追加された構造体**:
  - `WatcherState`: 現在有効な `RecommendedWatcher` を `Mutex` で包んでスレッドセーフに保持する共有状態。
- **追加された Tauri コマンド**:
  - `start_watching(path: String)`:
    - 指定されたパスに対して `RecursiveMode::Recursive` で再帰的なファイル監視を開始。
    - 既存の監視インスタンスが存在する場合は、自動的にドロップ（監視停止）してクリーンアップ。
    - 監視イベント検出時、変更パスに `.git` または `node_modules` を含むイベントを自動フィルタリングで除外した上で、フロントエンドに `file-system-change` イベントを通知（`Emitter::emit` を使用）。
  - `stop_watching()`:
    - 共有状態内のウォッチャーを `None` にリセットし、安全にファイル監視スレッドをドロップして監視を終了。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `notify = "6.1.1"` クレート, `tauri::Emitter`, `WatcherState` (共有状態)
  - **影響範囲 (Impact Scope)**: バックエンドのライフサイクル全体。コマンド呼び出しにより監視の起動と破棄が行われます。

### Rustバックエンド: アプリ起動時のセットアップ (`lib.rs`)
- **概要**: `WatcherState` の共有登録と、監視コマンドの Tauri ハンドラ登録。
- **変更内容**:
  - `.manage(WatcherState { watcher: std::sync::Mutex::new(None) })` により、アプリ起動時に状態を登録。
  - `invoke_handler` に `start_watching` と `stop_watching` を追加。

### フロントエンド: リアルタイムビュー自動同期 (`MainLayout.tsx`)
- **概要**: 選択されたプロジェクトパスの変更を検知してバックエンド監視を起動し、通知を受け取ってファイルツリーや履歴グラフを自動更新する処理を追加。
- **変更内容**:
  - `useEffect` を追加し、`projectPath` の変化を監視。
  - フォルダ変更時に `start_watching` を呼び出し、アンマウント時やフォルダ切り替え時に `stop_watching` で後片付けを実施。
  - `listen("file-system-change", ...)` でバックエンドからの変更通知を購読。
  - 複数ファイルの同時変更（ビルド処理等）による過剰な読み込みを防ぐため、`500ms` のデバウンス（防振）処理を実装した上で、`setHistoryRefreshKey(prev => prev + 1)` をトリガーしてビュー全体を同期リロード。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `@tauri-apps/api/event` (`listen`, `UnlistenFn`), `projectPath`, `setHistoryRefreshKey`
  - **影響範囲 (Impact Scope)**: アプリのメイン画面（ファイルツリー、履歴リスト、Gitグラフ）。ファイル変更に伴い、手動リロードなしで即座にビューが最新状態に同期します。

---

## 25. 変更・追加された仕様 (2026-05-22 アプリ名からapp-排除およびルート.gitignoreの完全ホワイトリスト化)

### プロジェクト設定: アプリ名の統一と「app-」の排除
- **概要**: フォルダの配置構成に由来していた `app-file-timemachine` という暫定的な名称を廃止し、正規の名前である `file-timemachine`（日本語名「ファイルタイムマシン」）に統一。
- **変更内容**:
  - `app-file-timemachine/src-tauri/tauri.conf.json`:
    - `productName` を `ファイルタイムマシン` に変更（インストーラーやショートカット名が日本語化されます）。
    - `identifier` を `com.yuzukounderson.file-timemachine` に変更。
    - `title`（ウィンドウタイトル）を `ファイルタイムマシン` に変更。
  - `app-file-timemachine/src-tauri/Cargo.toml`:
    - `name`（パッケージ名）を `file-timemachine` に変更。
    - `name`（ライブラリ名）を `file_timemachine_lib` に変更。
  - `app-file-timemachine/package.json`:
    - `name` を `file-timemachine` に変更。
  - `app-file-timemachine/src-tauri/src/main.rs`:
    - ライブラリ名変更に伴い、呼び出しを `app_file_timemachine_lib::run()` から `file_timemachine_lib::run()` に変更。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `file_timemachine_lib`
  - **影響範囲 (Impact Scope)**: アプリ起動用バイナリのエントリポイント（`main.rs`）。これによりライブラリのロードエラーが解消され、バイナリビルドが正常に行えるようになります。

### リポジトリ管理: ルート `.gitignore` の完全ホワイトリスト化
- **概要**: `app-file-timemachine` 配下のすべてを一度例外許可する大雑把な定義を廃止し、ビルドや開発に必要なリソース（ソースコードや各種設定ファイル）だけをピンポイントで明示的に例外許可（`!`）する「完全ホワイトリスト方式」に移行。
- **変更内容**:
  - ルート `.gitignore` から `!app-file-timemachine/**` を削除。
  - `package.json`, `tsconfig.json`, `vite.config.ts`, `src/` 配下、`public/` 配下、`src-tauri/` 配下の `Cargo.toml`, `tauri.conf.json`, `src/` 配下、`capabilities/` 等、ビルドに絶対必要なもののみを個別に許可（`!`）する記述にリニューアル。
- **影響範囲 (Impact Scope)**: リポジトリ管理全体。これにより、サブフォルダ内の `node_modules` や `target`（Rustビルド成果物）、`dist`（Viteビルド成果物）のほか、一時的に生成される `build.log` などのログファイルやエディタの一時ファイルが、ルートの `.gitignore` レベルで完璧にGitから除外され、混入が100%防止されます。

---

## 26. 変更・追加された仕様 (2026-05-22 多言語避難先英語化およびウィザードのデバッグ機能削除)

### 多言語対応: 非対応言語時の避難先（フォールバック）の英語化
- **概要**: サポートしていない言語（例：フランス語、ドイツ語など）のPC環境でアプリが起動された際の、初期表示（フォールバック先）を日本語（`ja`）から英語（`en`）に変更。自動言語検出機能（`LanguageDetector`）等の標準挙動はそのまま維持。
- **変更内容**:
  - `app-file-timemachine/src/i18n/config.ts`:
    - `fallbackLng` を `'ja'` から `'en'` に変更。
- **影響範囲 (Impact Scope)**: サポート外言語を使用する環境での初回起動時のUI言語。

### 環境セットアップ: ウィザード画面におけるデバッグ用シミュレーショントグルの完全削除
- **概要**: 依存関係診断ウィザードから、開発段階で利用していたデバッグ用の「シミュレーションモード切り替えトグル（`wizard-simulation-toggle`）」とその制御ロジック（`isSimulated`）を完全に削除。一般ユーザー配布用に向けた完全な製品版状態へ整理。
- **変更内容**:
  - `app-file-timemachine/src/components/setup/Wizard.tsx`:
    - `isSimulated` ステートを削除。
    - シミュレーション切り替えトグルのUI（`Simulation Toggle`）を削除。
    - `checkDeps` および `handleInstall` のバックエンド呼び出し引数 `simulate` を常に `false` に固定。
  - `app-file-timemachine/src/components/setup/Wizard.css`:
    - `.wizard-simulation-toggle` 関連のスタイル定義を削除。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `check_dependencies`, `install_dependency` (Tauri commands)
  - **影響範囲 (Impact Scope)**: 初回起動時のセットアップウィザード画面。これにより開発用のシミュレーション機能が取り除かれ、常に本物のGit・GitHub CLI環境の診断とインストールが動作するクリーンな本番環境用UIとなります。

---

## 27. 変更・追加された仕様 (2026-05-22 20カ国語への対応拡張)

### 多言語対応: エストニア語、オランダ語、ポーランド語、スウェーデン語の新規統合
- **概要**: 既存の16言語対応から、さらにグローバル展開を進めるため「エストニア語 (`et`)」「オランダ語 (`nl`)」「ポーランド語 (`pl`)」「スウェーデン語 (`sv`)」の4言語を新たに追加し、合計20カ国語対応に拡張。
- **変更内容**:
  - `app-file-timemachine/src/i18n/config.ts`:
    - `supportedLngs` 配列の末尾に `'et'`, `'nl'`, `'pl'`, `'sv'` を追加。
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx`:
    - `languages` 配列に `{ code: "et", name: "Eesti" }`, `{ code: "nl", name: "Nederlands" }`, `{ code: "pl", name: "Polski" }`, `{ code: "sv", name: "Svenska" }` を追加。
  - `app-file-timemachine/public/locales/` 配下:
    - `et/translation.json` (エストニア語), `nl/translation.json` (オランダ語), `pl/translation.json` (ポーランド語), `sv/translation.json` (スウェーデン語) の4つの翻訳リソースファイルを新規作成。全336行の翻訳キーを漏れなくローカライズ。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `i18next`, `SettingsModal.tsx`, `config.ts`
  - **影響範囲 (Impact Scope)**: 設定画面の言語切り替えプルダウンリスト、およびアプリ全体のローカライズ。これによりユーザーは新たに4カ国語を追加した合計20カ国語の選択肢から言語を自在に切り替えられるようになります。

---

## 28. 変更・追加された仕様 (2026-05-22 GitHubリモート連携)

### リポジトリ管理: GitHub プライベートリポジトリとの初回同期
- **概要**: ローカルのソースコードおよびコミット履歴をオンラインで安全に管理・バックアップするため、GitHub上にプライベートリポジトリを新規作成し、初回同期（プッシュ）およびメタデータの登録を実施。
- **設定内容**:
  - **リポジトリURL**: `https://github.com/DovahkiinYuzuko/file-timemachine`
  - **公開範囲**: プライベート（非公開）
  - **ディスクリプション**:
    `[ENG] A visual Git GUI desktop application for intuitive file history tracking and restoration. / [JPN] 直感的なファイルの変更履歴の追跡と復元を可能にする、すべての人のためのビジュアルGit GUIデスクトップアプリ。`
  - **トピック (Topics)**: `tauri`, `react`, `typescript`, `rust`, `git-gui`, `localization`, `desktop-app`
- **影響範囲 (Impact Scope)**: リポジトリの管理状態。ローカルリポジトリのリモートとして `origin` が追加され、以降はクラウドバックアップおよびCI/CDパイプラインとの連携が可能になります。

---

## 29. 変更・追加された仕様 (2026-05-22 不要フォルダ・ファイルのGit追跡解除)

### リポジトリ整理: 不要なキャッシュおよびドキュメント類のGit管理対象からの除外
- **概要**: Gitのtracked仕様（一度コミットされたファイルは `.gitignore` の影響を受けずに追跡され続ける挙動）を解消するため、ローカルの実ファイルは保持したまま、インデックスから不要なファイル・フォルダを安全に除外。
- **実行内容**:
  - `git rm -r --cached .history/` (ローカル履歴キャッシュフォルダの追跡解除)
  - `git rm -r --cached docs/` (開発者ドキュメントフォルダの追跡解除)
  - `git rm --cached .file-timemachine.json` (アプリ設定ファイルの追跡解除)
  - `git rm --cached ハリボテリスト.md` (不要ドキュメントの追跡解除)
  - `git rm --cached ファイルタイムマシンAAUI.md` (不要ドキュメントの追跡解除)
  - `git rm --cached ファイルタイムマシン仕様書.md` (不要ドキュメントの追跡解除)
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: ルート `.gitignore` の完全ホワイトリスト設定（例外許可されていない上記ファイル群は、追跡解除に伴い自動的かつ即座に完全無視されます）。
  - **影響範囲 (Impact Scope)**: リポジトリ構造のクリーン化。GitHub上から不要なフォルダやドキュメントが完全に消去され、リポジトリが軽量かつ必要なソースコードのみの構成に整理されます。

---

## 30. 変更・追加された仕様 (2026-05-22 Git自動追跡除外（rm）機能の実装)

### ファイルツリー: 無視登録時の自動 `git rm --cached` 連携
- **概要**: 過去に一度コミットされてGitの追跡対象（Tracked）になってしまっているファイルを、アプリ上のチェックボックス操作で「無視する」に設定した際、`.gitignore` の更新と同時に、自動的にGitのインデックス（追跡対象）からキャッシュを削除（`git rm --cached`）する機能を実装。
- **変更内容**:
  - `app-file-timemachine/src-tauri/src/commands/git.rs` (`update_gitignore`):
    - `is_ignored = true` の分岐処理内に、`git rm -r --cached --ignore-unmatch <relative_path>` を実行する子プロセス処理を挿入。
    - `--ignore-unmatch` フラグを付与することにより、ファイルがGitの追跡対象になっていなくてもエラーを発生させず、安全に処理が完了するように対応。
- **依存関係マッピング**:
  - **依存関係 (Dependencies)**: `std::process::Command` によるGitコマンド実行。フロントエンドの `FileTree.tsx` における `handleToggleIgnore` の呼び出し。
  - **影響範囲 (Impact Scope)**: ファイル無視管理の自動化。これにより、すでにGit上にアップロードされてしまっている不要ファイルやフォルダを、ローカルファイルを物理的に消すことなく、アプリ上でチェックを外すだけで即座にGitのインデックスから安全に除外して無視対象にできるようになり、GitHubリモート側からも綺麗に消去されるようになります。

### リポジトリ整理: ルート `.gitignore` の競合クリーンアップと不要ファイル追跡解除
- **概要**: ルートの `.gitignore` で `!app-file-timemachine/**` を例外許可していたために、サブディレクトリ `app-file-timemachine/` 配下の一時ファイルやビルド成果物（`build_error.txt`, `build_output.txt` など）がGit追跡対象になってしまっていた競合問題を、ルート `.gitignore` にブラックリストルールを追加することで根本解決。
- **変更内容**:
  - `.gitignore` (Root):
    - `app-file-timemachine/node_modules/`, `app-file-timemachine/dist/`, `app-file-timemachine/build_error.txt`, `app-file-timemachine/build_output.txt`, `app-file-timemachine/build.log`, `app-file-timemachine/src-tauri/target/`, `app-file-timemachine/src-tauri/gen/` などの除外ルールを追記。
  - **コマンド実行**:
    - `git rm --cached app-file-timemachine/build_error.txt`
    - `git rm --cached app-file-timemachine/build_output.txt`
    を実行し、インデックスから不要ファイルを即座に除外。
- **影響範囲 (Impact Scope)**: リポジトリ構成の完全クリーン化。これにより、ビルドエラーログやターゲットディレクトリがGitに混入する問題が完全に防止されます。

---

## 31. 変更・追加された仕様 (2026-05-23 本番EXE不具合・ツリー高速化更新)

### セキュリティ方針 (tauri.conf.json) の緩和
- **ファイルパス**: `app-file-timemachine/src-tauri/tauri.conf.json`
- **概要**: ビルド後の本番環境（EXE版）で、GitHubのアイコン（アバター画像）がロードできなかったり、GitHub APIとの通信に失敗して「GitHubとの通信に失敗しました」と表示されるTauri特有のCSPセキュリティ制限を解決するため、コンテンツセキュリティ方針（CSP）を適切に緩和。
- **変更内容**:
  - `app.security.csp` に以下を追加・緩和：
    - `img-src` に `https://avatars.githubusercontent.com` および `https://*` を追加（GitHubアバター等の外部画像を許可）。
    - `connect-src` に `https://api.github.com` を追加（GitHubのREST APIへの直接通信を許可）。
- **影響範囲 (Impact Scope)**: アプリ全体のGitHub連携機能（本番EXEビルドのみ影響）。

### フロントエンド: Windowsプレビュー画像のパス正規化
- **ファイルパス**: `app-file-timemachine/src/components/preview/FilePreview.tsx`
- **概要**: Windowsの本番環境において、ローカルのプレビュー画像を表示する際に、パスの区切り文字であるバックスラッシュ（`\`）がWebViewプロトコルの解釈を妨げ画像が壊れてしまうバグを修正。
- **変更内容**:
  - `convertFileSrc` に渡す `filePath` に対し、すべてのバックスラッシュをスラッシュに置換する処理を追加： `filePath.replace(/\\/g, "/")`
- **依存関係マッピング**:
  - **依存元**: `filePath` prop, Tauriの `convertFileSrc` API。
  - **影響範囲**: `FilePreview.tsx` コンポーネント内の画像プレビュー機能（Windows環境）。

### Rustバックエンド: Git無視連動型の超高速ファイルツリー走査
- **ファイルパス**: `app-file-timemachine/src-tauri/src/commands/files.rs`
- **概要**: プロジェクトディレクトリ内のファイル走査（`get_file_tree`）およびアプリ起動時の自動フォルダ復元が、巨大プロジェクトやキャッシュフォルダ（`node_modules` や `dist` など）の走査により極端に遅くなり、アプリがフリーズしていた問題を解決。
- **変更内容**:
  - `git status --ignored --porcelain=v1` コマンドを実行し、プロジェクト内で無視されているディレクトリの一覧（`ignored_dirs`）を一括取得する処理を追加。
  - `WalkDir` での走査中、巡回したエントリがディレクトリであり、かつ上記の `ignored_dirs` に含まれる場合に、 `it.skip_current_dir()` を実行して配下の探索を一撃でスキップするよう実装。
  - これにより、特定のフォルダ名（`dist` や `venv`）をアプリ側で勝手に決め打ち除外するのではなく、ユズコ自身が設定した `.gitignore` と100%同調させつつ、巨大なサブディレクトリの無駄なファイル走査を徹底的に排除した「Git無視連動型高速化」を実現。
- **依存関係マッピング**:
  - **依存元**: `walkdir` クレート（`WalkDir`）、 `std::process::Command`（`git status` コマンドの呼び出し）。
  - **影響範囲**: アプリ起動時の前回フォルダ自動復元処理、サイドバー of ファイルツリー（`FileTree.tsx`）のレンダリング。体感読み込み時間が100倍以上高速化され、巨大リポジトリでも一瞬で表示可能になります。

---

## 32. 変更・追加された仕様 (2026-05-23 プレビュー・監視不具合解消および同期UI・ログのフォーマル化)

### セキュリティ方針 (tauri.conf.json) の追加緩和
- **ファイルパス**: `app-file-timemachine/src-tauri/tauri.conf.json`
- **概要**: Windowsの本番環境（WebView2）において、アセット変換（`convertFileSrc`）により生成されたローカル動画や画像のURL（`http://asset.localhost`）が、セキュリティブロック（CSPエラー）によって読み込まれず、動画再生ができない状態を解消。
- **変更内容**:
  - `app.security.csp` に `http://asset.localhost` を追加し、`img-src` および `media-src` での読み込みを許可：
    - 旧: `media-src 'self' asset:;`
    - 新: `media-src 'self' asset: http://asset.localhost;`
- **影響範囲 (Impact Scope)**: 本番ビルドにおけるローカル動画および音声アセットのプレビュー再生。

### フロントエンド: 画像プレビューのRust経由base64表示方式への移行
- **ファイルパス**: `app-file-timemachine/src/components/preview/FilePreview.tsx`
- **概要**: Windows本番環境におけるローカル画像の表示バグ（WebView2によるプロトコルのブロックやキャッシュバグ）を100%確実に回避するため、最新画像アセットも過去コミットの表示と同様に「Rustバックエンドでファイルを読み込んでbase64エンコードし、インラインデータとしてフロントエンドに流す」方式へ完全移行。
- **変更内容**:
  - 最新ファイルの初回取得時およびポーリング監視時において、ファイルタイプが `image` の場合にも `read_file_content` コマンドを呼び出してbase64エンコード文字列を取得するよう修正。
  - `assetUrl` の生成ロジックを変更し、画像であれば過去コミットか最新かを問わず常に `data:${mime_type};base64,${textContent}` を使用するように統一。これによりキャッシュバグも完全に解消。
- **依存関係マッピング**:
  - **依存元**: Rustコマンド `read_file_content`（`preview.rs`）。
  - **影響範囲 (Impact Scope)**: アプリ上の画像ファイルプレビュー（すべての拡張子の画像）。

### フロントエンド: フォルダ自動監視とファイルツリーの完全連動
- **ファイルパス**: `app-file-timemachine/src/components/tree/FileTree.tsx`, `app-file-timemachine/src/components/layout/MainLayout.tsx`
- **概要**: バックエンド（`watcher.rs`）がフォルダ内の変更イベント（`file-system-change`）を受信してフロントエンドに発火していても、ファイルツリーを表示するコンポーネントが更新を検知せず再描画していなかったバグを解消。
- **変更内容**:
  - `FileTree.tsx`:
    - `FileTreeProps` インターフェースに `refreshKey?: number` を追加。
    - `useEffect` の依存配列に `refreshKey` を追加し、更新キーの増分をトリガーに自動的にファイルツリーと設定情報を再読み込み（`fetchTree`, `fetchConfig`）するように修正。
  - `MainLayout.tsx`:
    - レンダー内の `<FileTree>` の呼び出し部分に `refreshKey={historyRefreshKey}` を渡すように追記し、監視イベントとツリーの再取得ロジックを同期。
- **依存関係マッピング**:
  - **依存元**: `historyRefreshKey` ステート（`MainLayout.tsx`）、`file-system-change` イベントハンドラ。
  - **影響範囲 (Impact Scope)**: ファイルツリー（`FileTree.tsx`）の自動リアルタイム更新。

### ポップアップとシステムログのフォーマル化および多言語化 (i18n化)
- **ファイルパス**: 
  - `app-file-timemachine/public/locales/ja/translation.json`
  - `app-file-timemachine/src/components/layout/MainLayout.tsx`
  - `app-file-timemachine/src/App.tsx`
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx`
  - `app-file-timemachine/src/components/settings/SyncSettingsModal.tsx`
- **概要**: アプリ全体のUIにおける「System Messaging」およびログメッセージをフォーマルな丁寧語トーンに統一し、ソースコードへの日本語の直接書き込みを排除。
- **変更内容**:
  - `translation.json`: `layout.alert` 以下に `sync_success`, `sync_no_folder`, `sync_link_required`, `sync_prep_failed` のフォーマルなメッセージ定義を追加。
  - `MainLayout.tsx`: 同期処理関連のすべての直書き `alert` を、新設した多言語リソースを使った `t(...)` の呼び出しに変更。
  - `App.tsx`, `SettingsModal.tsx`, `SyncSettingsModal.tsx`: 各種ログメッセージ（`logger.info` や `logger.debug` など）に混入していたフランクなギャル表現を、完全にフォーマルな文章（丁寧語）に書き換え。
- **影響範囲 (Impact Scope)**: 同期処理実行時のダイアログメッセージ、およびデバッグコンソールへのシステムログ出力。

---

## 33. 変更・追加された仕様 (2026-05-23 ログメッセージの完全フォーマル化および同期アラートの多言語化最終検証)

### 多言語対応: 除外エラーおよび同期エラーアラートの多言語化
- **ファイルパス**: 
  - `app-file-timemachine/public/locales/ja/translation.json`
  - `app-file-timemachine/public/locales/en/translation.json`
- **概要**: `alert` で直書き表示されていた除外処理エラーと同期エラーメッセージを多言語化し、ビジネス品質のフォーマル表記に統合。
- **変更内容**:
  - `ja/translation.json`: `layout.alert` 以下に `"exclude_failed": "対象ファイルの除外処理に失敗しました：{{error}}"` および `"sync_failed": "クラウドとの同期に失敗しました：{{error}}"` を追加。
  - `en/translation.json`: 同様に `"exclude_failed": "Failed to exclude files: {{error}}"` および `"sync_failed": "Failed to sync with cloud: {{error}}"` を追加し、さらに前回の漏れであった `sync_success` などの他の同期系翻訳キーも網羅。
- **依存関係マッピング**:
  - **依存元**: フロントエンドの `MainLayout.tsx` からの `t(...)` 呼び出し。
  - **影響範囲 (Impact Scope)**: ファイル除外処理およびクラウド同期が失敗した際のユーザーポップアップアラート。

### フロントエンド: 直書きシステムログメッセージの完全フォーマル化
- **ファイルパス**: 
  - `app-file-timemachine/src/components/settings/SettingsModal.tsx`
  - `app-file-timemachine/src/components/setup/Wizard.tsx`
  - `app-file-timemachine/src/components/layout/Sidebar.tsx`
  - `app-file-timemachine/src/components/layout/MainLayout.tsx`
- **概要**: 過去の開発段階で残存していた内部デバッグログおよびインフォログ（`logger.debug`, `logger.info`, `logger.error`）内の「〜したよ」「〜するよ」といったすべてのギャル語表現を完全に排除し、フォーマルな丁寧語（敬体）へリファクタリング。
- **変更内容**:
  - `SettingsModal.tsx`: 設定の読み込み・一括保存時のログや、モーダル開閉、ボタンクリック時のログ（計10箇所）を「〜しました」「〜します」に完全変更。
  - `Wizard.tsx`: 依存関係の診断開始・エラー発生時のログ（計3箇所）をフォーマルに変更。
  - `Sidebar.tsx`: フォルダ選択ダイアログの開閉やタブクリック時のログ（計5箇所）をフォーマルに変更。
  - `MainLayout.tsx`: 同期エラー時の `alert(error)` を多言語化キーによる `alert(t("layout.alert.sync_failed", { error }))` に差し替え。
- **影響範囲 (Impact Scope)**: アプリ動作中のコンソールログ出力全体。これにより、一般ユーザー向け製品としても一切の砕けた表現がコンソールに残らない、100%プロフェッショナルなシステムメッセージ環境が実現しました。
